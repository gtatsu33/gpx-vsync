================================================================
  gpx-vsync - 実装仕様書
  版数: 2.6  作成日: 2026-07-12（初版）  更新日: 2026-07-19
  実装方式: Python 3.11+ / PySide6
================================================================

1. 技術スタック
----------------------------------------------------------------

  言語:          Python 3.11以上
  GUIフレームワーク: PySide6 (6.11以降)
                 ※ 2026-07-19、PyQt6から移行（Step29）。QtLocationの
                 OSMプラグインでの地図ネイティブ化のため、QtLocation
                 バインディングを持つPySide6を採用した
  マップ表示:     PySide6のQtLocation（QtQuickWidget + QML、OSMプラグイン）
                 ※ 2026-07-19、Step30でLeaflet.js+QWebEngineViewから
                 置き換え（2章・9章参照）
  動画再生:      PySide6.QtMultimedia (QMediaPlayer + QVideoWidget)
                 ※ PySide6本体（Addonsパッケージ）に同梱。
                 別パッケージのインストールは不要
  GPX処理:       gpxpy 1.6.2
  動画トリミング:  ffmpeg (subprocess経由)
  動画へのGPS埋め込み: GPAC / MP4Box (subprocess経由)
  マップマッチング: requests（HTTP通信）+ Valhalla trace_attributes API
                   （外部公開デモサーバー、要インターネット接続。13章）

  依存パッケージ:
    pip install PySide6 gpxpy requests
    ※ PySide6メタパッケージがQtWebEngine/QtMultimedia等（旧
    PyQt6-WebEngine相当）を含むAddonsも併せてインストールするため、
    個別パッケージの追加インストールは不要

  外部ツール（別途インストール）:
    brew install ffmpeg
    brew install gpac             ← MP4Boxコマンドを提供、CAMMトラック埋め込みに使用
    pip install mapillary_tools   ← アップロード時に手動で使用 ＋ 出力後の
                                     ローカル検証（5-10章・14章）に自動使用。
                                     ffmpeg/GPACと異なり必須ではない任意ツール
                                     （未インストールなら検証をスキップするのみ）

  参考バージョン確認日: 2026-07-12（PyPI直接確認、PyQt6時代の記録）
    - PyQt6: 6.11.0 (PyPI)
    - PyQt6-WebEngine: 6.11.0 (PyPI)
    - gpxpy: 1.6.2 (PyPI, github.com/tkrajina/gpxpy)

  参考バージョン確認日: 2026-07-19（PyPI直接確認、PySide6移行時）
    - PySide6: 6.11.1 (PyPI)。QtLocation/QtPositioningは
      PySide6-Addonsに含まれ、`pip install PySide6`のメタ
      パッケージ経由で自動的に入る（PySide6-Essentials/Addons分割
      構成、doc.qt.io/qtforpython-6/package_details.html で確認）
    - mapillary_tools: 0.14.7 (github.com/mapillary/mapillary_tools)
      ※ mapillary_toolsのバージョンは実際の連携時に再確認すること

  参考ドキュメント確認日: 2026-07-13（公式ドキュメント直接確認）
    - Valhalla trace_attributes API仕様: valhalla.github.io/valhalla の
      API Reference（map-matching）を確認。shape/costing/shape_match/
      trace_options/filters の各パラメータ名、matched_pointsレスポンス
      （type: matched/interpolated/unmatched）を検証済み（13章）
    - valhalla1.openstreetmap.de（FOSSGIS e.V.運営の公開デモサーバー）の
      fair-useポリシー: 1ユーザーあたり1req/秒、全体で100req/秒の
      レート制限。公開アプリからの利用時は X-Client-Id ヘッダーでの
      識別が推奨されている（13章で対応）

  注記: implement.txt初版で「PyQt6-Qt6-Multimedia」という別パッケージ
  名を記載していたが、PyPI上に存在しないパッケージ名であることが
  判明したため削除した。QtMultimediaはPyQt6本体に含まれる。


2. マップ表示の実装方針
----------------------------------------------------------------

  Qtのネイティブマップ機能（QtLocation）は、当初採用していたPyQt6には
  Pythonバインディングが存在しないため（Qt Forum・実機のvenv調査で
  確認済み）、「QWebEngineView + Leaflet.js」の組み合わせを採用していた
  （旧実装。9章の履歴参照）。

  2026-07-19、GUIフレームワークをQtLocationバインディングを持つ
  PySide6へ移行（Step29、1章参照）した上で、地図表示自体もQtLocationの
  OSMプラグイン（QML）によるネイティブ実装へ置き換えた（Step30）。
  QWebEngineView（Chromium同梱）への依存を解消し、将来.appとして
  再配布する際のバイナリサイズ肥大化を避けることが目的。

  具体的には:
  1. アプリ起動時に app/map_widget.py（QQuickWidget継承）が
     assets/map.qml をロードする
  2. GPXルート・現在地マーカーの更新はPython側から
     QQuickWidget.rootObject() 経由でQML側のfunction
     （loadRoute/updateRouteRanges/updateMarker/hideMarker/clearMap）
     を直接呼び出して行う（詳細は9章）

  OSMタイルURL:
    https://tile.openstreetmap.org/{z}/{x}/{y}.png
    Attribution: © OpenStreetMap contributors (表示必須)

  重要な注意点（実機検証で判明。Qt 6.11）:
    QtLocationの`osm`プラグインは、デフォルトのactiveMapTypeがQt社の
    プロキシ経由でThunderforestのタイル（APIキー要求の透かし入り）を
    使う設定になっており、単純に`Plugin{name:"osm"}`を指定しただけでは
    上記のOSM直接タイルにはならない。`osm.mapping.custom.host`
    プラグインパラメータで独自のCustomMapタイプを追加し、
    `Map.activeMapType`をそのタイプへ明示的に切り替える処理が必要
    （9章のmap.qml参照）。またQtLocationの`Map`型はLeafletと異なり
    attribution（著作権表示）を自動描画しないため、QML側で手動の
    オーバーレイ表示を追加している（OSMタイル利用ポリシー上必須）。

  地図の操作性（実機検証で判明。Step30-9、2026-07-19）:
    QtLocationの`Map`型は、Leafletと異なりデフォルトではマウス/
    トラックパッド操作でのパン・ズームに対応していない（Qt公式
    ドキュメント doc.qt.io/qt-6/qml-qtlocation-map.html のInteractivity
    サンプルで確認）。`PinchHandler`（ピンチズーム・回転）、
    `WheelHandler`（マウスホイールズーム）、`DragHandler`（ドラッグ
    パン）をMap要素に明示的に追加する必要がある（9章のmap.qml参照）。


3. ディレクトリ構成
----------------------------------------------------------------

  video_gpx_sync/
  ├── main.py               # エントリポイント
  ├── app/
  │   ├── __init__.py
  │   ├── state.py          # AppState: アプリケーション状態のデータクラス
  │   ├── main_window.py    # QMainWindow: 全体レイアウト管理
  │   ├── theme.py          # 配色パレット・QSS定義、ライト/ダーク自動切替（16章）
  │   ├── map_widget.py     # QQuickWidget ラッパー + QtLocation(QML)連携
  │   ├── video_widget.py   # QMediaPlayer + QVideoWidget + タイムライン
  │   ├── offset_widget.py  # GPSオフセット調整ボタン群
  │   ├── timelapse_widget.py  # タイムラプス設定（チェックボックス＋間隔入力、15章）
  │   ├── time_utils.py     # playback_ms_to_real_ms(): 再生位置→実世界経過時間変換（15章）
  │   ├── gpx_handler.py    # gpxpy を使ったGPX読み書き・時刻補間
  │   ├── video_handler.py  # ffmpeg subprocess呼び出し、メタデータ取得
  │   ├── camm_encoder.py   # GPAC/MP4Box経由でCAMM GPSトラックの埋め込み・読み出し
  │   ├── exporter.py       # 動画出力処理
  │   ├── map_matcher.py    # Valhalla APIでのマップマッチング、fair-use対応レート制限（13章）
  │   ├── map_matching_worker.py  # QThreadでmap_matcher処理をバックグラウンド実行
  │   ├── map_matching_dialog.py  # マップマッチング進捗ダイアログ（プログレスバー＋キャンセル）
  │   ├── mapillary_validator.py  # mapillary_tools video_processによる出力後ローカル検証（14章）
  │   ├── mapillary_validation_worker.py  # QThreadでmapillary_validator処理をバックグラウンド実行
  │   └── mapillary_validation_dialog.py  # ローカル検証中ダイアログ（不定形インジケーター＋キャンセル）
  ├── assets/
  │   ├── map.qml            # QtLocation(OSMプラグイン)を使った地図QML
  │   └── icons/
  │       └── check.svg     # チェックボックスのチェックマーク（白、17章）
  └── requirements.txt


4. 主要クラス設計
----------------------------------------------------------------

4-1. MainWindow (app/main_window.py)
  - QMainWindow を継承
  - 左右のペインを QSplitter で分割
  - 左: ヘッダー行（タイトル「マップ」＋open_gpx_button）+
    map_matching_status_label + MapWidget + OffsetWidget
  - 右: ヘッダー行（タイトル「動画」＋open_video_button）+ VideoWidget
  - 下部: 再生ボタン群 + 出力ボタン
  - open_gpx_button / open_video_button（17章）: 各カードヘッダーに
    配置するボタン。クリックでopen_gpx_dialog() / open_video_dialog()
    （メニューアクションと同じメソッド）を呼ぶ。open_video_buttonは
    23章（読み込み順序ルール撤廃）により常にenabled
    （2026-07-15変更。以前はGPX読み込み前はdisabledだった）
  - _update_open_button_emphasis()（17章・23章）: 未読み込み状態の
    ボタンをobjectName="primaryButton"にして強調表示する
    （_set_button_emphasis()ヘルパー経由。objectName変更後は
    style().unpolish()/polish()でQSSを再適用する必要があることを
    実機検証済み）:
      open_gpx_button: state.gpx_data is None の間 強調
      open_video_button: state.video_path is None の間 強調
        （2026-07-15変更。以前はstate.gpx_data is not None
        との連動条件だったが、23章で読み込み順序ルールを撤廃した
        ため、互いの読み込み状況に依存しない独立判定にした）
    __init__()の初期化時、load_gpx()末尾（_apply_gps_source()経由）、
    load_video()末尾で呼ぶ
  - アプリケーション状態を一元管理するステート:
      AppState（app/state.py で定義するデータクラス）として保持する
      video_path: str | None
      gpx_path: str | None    # 出力ファイル名生成のため元GPXパスも保持
      gpx_data: gpxpy.GPX | None
      video_creation_time: datetime | None  # 動画の録画開始時刻（絶対時刻、UTC）
      offset_seconds: float  # GPS時刻オフセット（秒）
      video_start_ms: int    # Startマーカー位置（ミリ秒）
      video_end_ms: int      # Endマーカー位置（ミリ秒）
      video_duration_ms: int
      video_time_scale: float  # 再生1msあたりの実世界ms数。通常動画は1.0
                                # （タイムラプス動画対応。15章）
  - GPX・動画の読み込みに順序制約は無い（23章、2026-07-15撤廃。
    以前はGPX未読み込みの間、動画読み込み関連UIを無効化していた）
  - dragEnterEvent/dropEvent（23章）: URLを持つドラッグは常に受け付け
    （acceptProposedAction()）、ドロップ時は拡張子が".gpx"ならload_gpx()、
    それ以外は常にload_video()を呼ぶ（動画かどうかの判定・エラー
    メッセージ表示はload_video()内のFFmpegError処理に委ねる。
    以前あった「GPX未読み込み時は動画ドロップを拒否」という
    分岐は撤廃した）
  - 「ツール」メニュー（旧VerificationWindow起動用。4-10節参照）は
    23章の対応で削除した
  - on_force_sync_clicked()（OffsetWidgetの強制同期ボタンから配線）:
      動画の作成日時とGPXの記録開始時刻が大きくずれているケース
      （±ボタンでの手動調整では実質補正不可能な、日付をまたぐ
      レベルのずれ）に対する安全策。
      gpx_handler または video_creation_time が無い場合は
      「先にGPXファイルと動画の両方を読み込んでください」の警告を
      表示して終了する（2026-07-15、23章の読み込み順序ルール撤廃に
      伴い文言変更。以前は「先に動画を読み込んでください」固定
      だったが、GPX側が無い可能性もあるため文言を汎用化した）。
      それ以外の場合:
        gpx_start_time = gpx_handler.get_all_points()[0].time
        video_start_true_time = video_creation_time
                                 + timedelta(milliseconds=
                                     playback_ms_to_real_ms(
                                         video_start_ms, state.video_time_scale))
        offset_sec = (gpx_start_time - video_start_true_time)
                     .total_seconds()
        offset_widget.set_offset(offset_sec)
      これにより、GPXデータ先頭のトラックポイント時刻と、動画
      タイムラインのStartマーカー位置（動画ファイル自体の0msでは
      なく、トリミング後に実際に書き出される先頭フレーム）が指す
      時刻を強制的に一致させる。video_time_scale（15章）が1.0以外
      （タイムラプス設定有効時）の場合は、再生位置msを実世界経過
      時間msに変換してから加算する。
  - load_gpx(path)（マップマッチング統合後のフロー。13章・4-6節・
    4-11節・4-12節・23章）:
      1. GPXHandler.load(path) でパース、既存のトラックポイント
         有無チェック（従来通り）
      2. MapMatchingWorker（4-12節）に handler.get_all_points() を渡し
         起動、MapMatchingDialog（4-12節）をモーダル表示する
      3. ワーカー完了後、結果の points（時刻・標高は元のまま、
         座標のみスナップ済み）で
         gpx_handler.replace_points(points)（4-6節）を呼び出す
      4. _apply_gps_source(handler, gpx_path=path)（下記、23章）を
         呼び、GPXデータを内部状態に反映する
      ワーカーがキャンセル・エラーになった場合も replace_points()
      自体は必ず呼ぶ（元座標、または部分的にスナップ済みの座標で
      継続する。5-9節）
      5. _update_map_matching_status(result, total_points=len(points))
         （17章）を呼び、結果をmap_matching_status_labelに反映する
  - _apply_gps_source(handler: GPXHandler, gpx_path: str)（23章、
    2026-07-15追加）: GPXデータを内部状態に反映する共通処理。
    load_gpx()（外部GPXファイル読み込み）と_try_load_embedded_gps()
    （動画のCAMM埋め込み抽出、下記）の両方から呼ばれる:
      self.gpx_handler = handler
      self.state.gpx_path = gpx_path
      self.state.gpx_data = handler.gpx
      map_widget.load_gpx_route([(p.latitude, p.longitude)
                                  for p in handler.get_all_points()])
      _update_export_button_state()
      _update_open_button_emphasis()
      _update_route_range_highlight()
      self.offset_widget.reset()  # 2026-07-18追加、下記
    注: マップマッチング結果の反映（_update_map_matching_status()）
    と動画埋め込みGPS採用の反映（_update_gps_source_status_for_camm()）
    はこのメソッドの外（呼び出し元）で行う。ステータス表示の文言が
    データの出所によって異なるため（5-9節・5-12節）。

    offset_widget.reset()を追加した経緯（2026-07-18、実機で発生した
    不具合の修正）:
    ユーザーより「GPXと動画を別々に読み込み同期を取ってExportした後、
    そのExport済みsynced動画を同一アプリ起動中に読み込み直すと同期が
    ズレる。アプリを再起動してから読み込めば正しく再生される」との
    報告を受けた。実際にtests/test_main_window.pyと同じ手順
    （load_gpx→load_video→on_offset_changed(2.0)→export→同一
    MainWindowインスタンスでload_video(synced_path)）で再現した結果、
    再読み込み後もstate.offset_secondsが直前の値（2.0）のまま
    リセットされておらず、gpx_handler.interpolate_position()に
    渡ると誤った座標になることを確認した。
    原因: Export済みの動画に埋め込まれたGPS（CAMM）は、Export時点で
    既にstate.offset_secondsを適用した後の実時刻が焼き込まれている。
    ところが_apply_gps_source()（_try_load_embedded_gps()経由で
    このExport済み動画自身から新しいgpx_handlerを構築する際にも
    呼ばれる）は、gpx_handler・gpx_path・gpx_dataを差し替えるだけで
    state.offset_secondsには一切触れていなかったため、既に
    オフセット適用済みのGPSデータに対して、古いセッションの
    オフセット値がそのまま二重に上書き適用されてしまっていた。
    AppState()は新規作成時offset_seconds=0.0がデフォルトのため、
    アプリを再起動すれば0から再開され問題が起きない、という点も
    実機報告と整合した。
    対処として、_apply_gps_source()の最後でoffset_widget.reset()を
    呼ぶようにした。GPSデータソース自体が切り替わった以上、直前の
    セッションで調整したオフセットは新しいデータに対してはもはや
    正しくない（特にExport済み動画の自己embedded GPSに対しては
    常に0が正しい）という考え方に基づく。OffsetWidget.reset()は
    内部状態を0にした上でoffset_changedシグナルを発行するため、
    on_offset_changed()経由でstate.offset_seconds・マップの現在地
    マーカー・出力ボタンの有効状態・ルート塗り分けも連動して更新
    される（新たな配線を追加する必要がなかった）。
    回帰テスト:
    test_reloading_own_exported_synced_video_resets_stale_offset
    （tests/test_main_window.py）で、offset=2.0でExportした動画を
    同一セッション内で読み込み直した後、state.offset_secondsが0.0に
    戻ること、および位置補間結果が正しい座標になることを確認した。
  - load_video(path)（タイムラプス対応統合後のフロー。15章・4-15節・
    4-16節・23章）:
      1. get_creation_time() / get_duration_ms() 取得、
         state.video_path/video_creation_time/video_duration_ms/
         video_start_ms/video_end_ms を設定（従来通り）
      2. state.video_time_scale = 1.0 にリセット
      3. video_handler.get_fps(path) を取得し self._video_fps に
         キャッシュする（FFmpegError時は None のまま。タイムラプス
         設定が有効化された際の倍率計算に使う）
      4. timelapse_widget.reset()（4-16節）を呼び、タイムラプス設定
         UIを無効状態に戻す（このreset()が発火するtimelapse_changed
         シグナル経由でstate.video_time_scale・再生レートも
         整合させる）
      5. video_handler.has_audio_stream(path) を判定する
         （FFmpegError時はTrue扱い＝ヒントを出さない、fail-safe）
      6. _prompt_timelapse_settings(has_audio) を呼び、
         「動画の設定確認」ダイアログを表示する（毎回・discoverability
         対応。下記）。戻り値(enabled, interval_sec)がenabled=Trueの
         場合、timelapse_widget.checkbox.setChecked(True) →
         timelapse_widget.interval_spinbox.setValue(interval_sec)を
         呼び、常設のタイムラプス設定コントロール（4-16節）にも
         同じ値を反映する（このsetChecked/setValueが発火する
         timelapse_changedシグナル経由でstate.video_time_scale・
         再生レートも整合する）
      7. _try_load_embedded_gps(path)（下記、23章、2026-07-15追加）
         を呼び、動画にCAMM埋め込みGPSがあればGPXデータとして採用する
      8. video_widget.load(path) → _apply_playback_rate()（下記）
         → _update_export_button_state()
  - _try_load_embedded_gps(path: str)（23章、2026-07-15追加。
    同日中にstate.video_creation_time上書き処理を追加）:
      try:
          camm_points = extract_gps_track(path)  # 4-8節
      except (CammTrackNotFoundError, CammEncodeError):
          return  # 埋め込みが無い動画は何もしない（既存GPSデータを維持）
      if not camm_points:
          return
      # MP4Boxの制約3（4-8節）によりcreation_timeがMP4Box実行時刻に
      # 化けている場合があるため、CAMM先頭サンプルのepoch_timeで
      # state.video_creation_timeを上書きする（実機で発生した不具合の
      # 修正。23章末尾の追記参照）
      first_point = min(camm_points, key=lambda p: p[0])
      state.video_creation_time = datetime.fromtimestamp(
          first_point[4], tz=timezone.utc)
      handler = GPXHandler.from_camm_points(camm_points)  # 4-6節
      _apply_gps_source(handler, gpx_path=path)
      _update_gps_source_status_for_camm(len(camm_points))
    「最後に読み込んだGPS付きファイルが常に優先される」ルール（23章）
    は、_apply_gps_source()が呼ばれるたびに無条件でgpx_handler/
    state.gpx_path/state.gpx_dataを上書きすることで実現している。
    CAMM埋め込みが無い動画の場合はこの関数自体が早期returnするため、
    既存のGPXデータ（外部GPXファイル由来・別動画のCAMM由来いずれも）
    は変更されない。
  - _update_gps_source_status_for_camm(point_count: int)（23章、
    2026-07-15追加）: 動画のCAMM埋め込みをGPXデータとして採用した
    際のステータス表示。マップマッチングは適用していないため、
    _update_map_matching_status()とは別の文言
    「✓ 動画に埋め込まれたGPSデータを使用（◯点）」を表示する
    （_set_map_matching_status_label()共通ヘルパー経由。5-12節）
  - _prompt_creation_time()（2026-07-15変更）: gpx_handlerが
    未読み込み（None）の場合はデフォルト値の算出をスキップし、
    現在時刻をデフォルトにする（23章。以前はgpx_handlerが必ず
    存在する前提のassertがあったが、動画がGPXより先に読み込まれる
    ケースがあるため撤廃した）
  - _prompt_timelapse_settings(has_audio: bool) -> tuple[bool, float]
    （15章。動画読み込み時に毎回表示するタイムラプス設定確認
    ダイアログ。UI上の常設タイムラプス設定コントロール（4-16節）が
    動画タイムラインの下という目立たない位置にあり、実際に
    「見つからない」というフィードバックを受けたことへの対応。
    2026-07-13の実機診断（headlessでMainWindowを起動しスクリーンショット
    取得）で常設コントロール自体は正しく描画されていることを確認した
    うえで、視認性の問題として本ダイアログを追加した）:
      - has_audio が False の場合のみ、「この動画には音声トラックが
        検出されませんでした。タイムラプス動画の可能性があります
        （自動判定ではないため確証はありません）。」というQLabelの
        ヒントをダイアログ先頭に追加する（旧版ではQMessageBox.information
        で別立てに表示していたが、アクション（チェックボックス）と
        切り離されていて見落とされやすかったため、本ダイアログに統合した）
      - QCheckBox「タイムラプス動画」（既定OFF）＋ QDoubleSpinBox
        「間隔(秒):」（既定0.5、チェック時のみ有効化。TimelapseWidgetの
        DEFAULT_INTERVAL_SEC・レンジ設定を再利用）
      - QDialogButtonBoxはOKボタンのみ（Cancelは設けない。既定値
        （タイムラプス無効）のまま進めても実害が無いため、確認せず
        閉じられる操作は用意しない設計とした）
      - dialog.exec() 完了後、チェックボックス・スピンボックスの
        最終状態をそのままタプルで返す（Acceptedかどうかによる分岐は
        行わない）
  - on_timelapse_changed(enabled, interval_sec)（TimelapseWidget.
    timelapse_changedから配線。15章）:
      enabled かつ self._video_fps が None でない場合:
        state.video_time_scale = interval_sec * self._video_fps
      それ以外: state.video_time_scale = 1.0
      続けて _apply_playback_rate()、
      on_position_changed(video_widget.timeline.position_ms())
      （プレビュー即時反映）、_update_export_button_state()、
      _update_video_info_label(interval_sec)（18章）を呼ぶ
  - _current_playback_rate() -> float（19章。2026-07-15追加）:
      state.video_time_scale == 1.0 の場合 1.0、それ以外は
      1.0 / state.video_time_scale を返す。_apply_playback_rate()と
      _on_reverse_tick()（19章）の速度計算ロジックを共通化したもの
  - _apply_playback_rate()（15章）:
      state.video_time_scale != 1.0 の場合:
        video_widget.player.setPlaybackRate(_current_playback_rate())
        video_widget.audio_output.setMuted(True)
      それ以外:
        video_widget.player.setPlaybackRate(1.0)
        video_widget.audio_output.setMuted(False)
      タイムラプス動画を見かけ上「間隔秒あたり1フレーム」で再生する
      ための処理。実機検証（Qt 6.11、FFmpegベースのマルチメディア
      バックエンド、macOS）で setPlaybackRate() に0.067倍速のような
      極端に遅い値を設定してもエラーなく動作し、position()が経過
      時間にほぼ比例して進行することを確認済み。
  - 出力ボタンのラベルは「📤 Export & Test」とする（5-6節・8章の
    レイアウト詳細も参照）
  - on_play_clicked() / on_pause_clicked()（旧on_stop_clicked。5-5節・
    19章。2026-07-13/2026-07-15変更）:
      いずれも冒頭で_stop_reverse_timer()（19章）を呼んでから、
      video_widget.play() / video_widget.pause() を呼ぶ。
      on_pause_clicked()はStartマーカー位置へのシークを行わない
      （旧仕様では停止＝一時停止＋Startへのシークだったが、
      「シークバーがドラッグできないため、途中で止めても先頭に
      戻ってしまうのは致命的」というフィードバックを受け、
      一時停止に徹する仕様に変更した。先頭に戻りたい場合は
      4-4節のシークバードラッグで代替する）
  - on_export_clicked()（5-10節・14章。出力後ローカル検証の統合）:
      0. _confirm_and_apply_gps_coverage_crop()（21章、2026-07-15追加）
         がFalseを返した場合は、その時点で処理全体を中断する
         （保存ダイアログも開かない）
      1. QFileDialog.getSaveFileName()で動画の保存先パス・ファイル名を
         取得する（22章、2026-07-15変更。以前はgetExistingDirectory()
         でフォルダのみ選択していた）。初期表示名は
         exporter.default_video_filename(state)、フィルタは
         "MP4 Files (*.mp4)"。キャンセル時（空文字列）は処理を中断する
      2. exporter.export(state, video_output_path) を呼び出し、動画・
         GPXパスを得る（4-9節。第2引数がoutput_dirから
         video_output_pathに変更されたことに伴い呼び出し側も変更）
      3. build_mapillary_tools_command() でコマンド文字列を組み立てる
         （従来通り）
      4. mapillary_validator.is_mapillary_tools_available() が
         True の場合のみ:
           MapillaryValidationWorker（14章）に video_path と
           video_start_time文字列を渡して起動、
           MapillaryValidationDialog（14章）をモーダル表示し、
           完了まで待つ（load_gpx()のマップマッチング統合と同じ
           パターン。4-12節）
         False の場合: 検証をスキップし、その旨を完了メッセージ
         に含める
      5. 完了ダイアログ（QMessageBox）に、動画・GPXパス、
         mapillary_toolsコマンド例に加えて、4.の検証結果
         （件数・エラー・警告、またはスキップ理由）を追記して表示

4-2. MapWidget (app/map_widget.py)
  - QQuickWidget を継承（2026-07-19、Step30でQWebEngineViewから置き換え。
    9章参照）。assets/map.qml をロードし、setResizeMode(
    SizeRootObjectToView)でウィジェットサイズに地図を追従させる
  - メソッド:
      load_gpx_route(points: list[tuple[float,float]])
        → QML側 loadRoute() を rootObject() 経由で直接呼び出し、
           MapPolylineでルートを描画（全区間を一旦「範囲内」扱いで
           描画する。9章参照）
      update_route_ranges(in_range: list[bool])
        → QML側 updateRouteRanges() を呼び、load_gpx_route()で
           描画済みのルートをin_range（各点が動画のStart/End出力
           範囲内かどうか）に応じて2色に塗り分け直す（20章）。
           座標は再送しない（QML側がloadRoute()時点で保持している）
      update_marker(lat: float, lon: float)
        → QML側 updateMarker() を呼び、MapQuickItemの座標更新+
           Map.centerへの追従（Leaflet版panTo相当）
      hide_marker()
        → QML側 hideMarker() を呼び、マーカーを非表示化
           （interpolate_position が None を返した場合に呼び出す）
      clear()
        → QML側 clearMap() を呼び、ルートとマーカーを消去

  - _is_ready / _pending_calls によるキューイング機構は維持しているが、
    QQuickWidgetはローカルQMLファイルの場合setSource()が同期的に
    Status.Readyへ遷移するため、通常の起動フローでは実質的に
    キューされることはない（防御的な実装として残している。9章参照）

  注: QQuickWidget（QtQuick描画）と QVideoWidget（4-3節、QtMultimedia
  描画）を同一ウィンドウ内で併用するため、main.py で QApplication
  インスタンス生成前に
  QCoreApplication.setAttribute(Qt.ApplicationAttribute.AA_ShareOpenGLContexts)
  を呼び出しておく必要がある（OpenGLコンテキスト共有の事前設定。
  Leaflet版のQWebEngineView時代から変わらず必要）。

4-3. VideoWidget (app/video_widget.py)
  - QWidget を継承
  - 構成要素:
      QVideoWidget       # 動画描画エリア
      QMediaPlayer       # 再生制御
      QAudioOutput       # 音声出力（ミュートでも必要）
      CustomTimeline     # カスタムタイムラインウィジェット（後述）
  - シグナル:
      position_changed(ms: int)  # 再生位置が変わるたびに発火

  - QMediaPlayer の使い方（Qt6最新版。PySide6/PyQt6でAPI共通）:
      self.player = QMediaPlayer()
      self.audio  = QAudioOutput()
      self.player.setAudioOutput(self.audio)
      self.player.setVideoOutput(self.video_widget)
      self.player.setSource(QUrl.fromLocalFile(path))
      self.player.positionChanged.connect(self.on_position_changed)

  - load(path)（2026-07-15変更、先頭フレームの即時表示）:
      setSource()の直後に以下を追加した:
        self.player.play()
        self.player.pause()
        self.player.setPosition(0)
      背景: 動画読み込み直後は何もデコードされておらず、再生ボタンを
      押すまでQVideoWidgetが真っ暗のままだった。play()→pause()で
      デコードパイプラインを一度動かし先頭フレームをレンダリングさせた
      上で、setPosition(0)で位置を0msに戻す。実機検証
      （Qt6.11、offscreenプラットフォーム）で、durationChangedを
      待たずにsetSource()の直後にこの3行を呼んでも（Qt内部で
      キューされ）正しく機能し、最終的にPausedState・position=0・
      有効な映像フレーム（QVideoWidget.grab()が非nullな320x240画像を
      返す）になることを確認済み。

  注（テスト時に判明した既知の問題）: QMediaPlayerにソースを
  setSource()した状態のままウィジェットを破棄すると、
  AVFoundationバックエンド（macOS）の後片付けがQtイベントループ外で
  ブロックし、プロセスがハングすることを実機検証（pytest-qt環境）で
  確認した。アプリ終了時や動画の差し替え時には、
  player.stop() → player.setSource(QUrl()) の順で明示的に
  解放してから次の処理に進むこと。通常のGUI操作（app.exec()の
  イベントループが常時回っている状態）では発生しにくいと見られるが、
  念のためMainWindowの終了処理（closeEvent等）でも同様の後片付けを
  行うことが望ましい。

4-4. CustomTimeline (app/video_widget.py 内)
  - QWidget を継承し、paintEvent で自前描画
  - 要素:
      - 全体バー（動画全長）
      - Start/End マーカー（ドラッグ可能な三角形ハンドル。バー上端から
        上向きに描画）
      - 現在位置インジケーター（青い縦線。POSITION_LINE_COLOR =
        QColor(37, 99, 235)。バー下端の下側に、Start/Endハンドルと
        同形・同サイズの三角形を上下反転させて追加している
        （2026-07-15追加。_draw_handle()に flip: bool = False
        パラメータを設け、flip=Trueの場合は頂点をバー下端の位置y、
        底辺をy+size側に描画することで上部ハンドルの鏡像にしている）。
        色はEND_HANDLE_COLOR（赤系）と紛らわしかったため、青系に
        変更した（2026-07-15）
      - 時刻ラベル（現在時刻、Start時刻、End時刻）
  - ドラッグ検出: mousePressEvent / mouseMoveEvent / mouseReleaseEvent
    - self._dragging は "start" / "end" / "position" / None の
      いずれか
    - Start/Endハンドルは、Y座標がバー上端（rect.top()）以下
      （＝上部三角マークの領域）の場合のみヒット判定を行う
      （2026-07-15修正。現在地の下向き三角マーク追加後、Y座標を
      見ずにX座標のみでStart/End判定していたため、バー下部や現在地
      マーカー付近をクリックしてもX座標がStart/Endに近ければ誤って
      Start/Endが動いてしまう不具合があった。Y座標の上限チェックを
      追加することで、上部三角マークの領域のみがStart/Endの
      ヒット領域になるようにした）
    - 上記のStart/Endヒット領域に該当しない場所（バー本体、および
      下部の現在地三角マークの領域）を押した場合は "position" として
      扱い、押した瞬間に一度 seek_requested を発火した上で、そのまま
      ドラッグを継続すると mouseMoveEvent のたびに連続して
      seek_requested を発火する（スクラブ操作。2026-07-13追加。
      以前は押した瞬間の1回しかシークされず、ドラッグに追従
      しなかった。ユーザーから「シークバーがドラッグできないのは
      致命的」というフィードバックを受けて対応した）。なお
      CustomTimeline自体はmouseMoveEventのたびに無条件で
      seek_requested を発火するが、これを受け取るVideoWidget.seek()
      側で2026-07-18に描画完了待ちのペーシングを追加したため、
      高速ドラッグ時は中間の位置が間引かれる（19章参照）
  - Start/End の制約:
      Start < End（MIN_GAP_MS=100ms以上の間隔を維持。_clamp_start()/
      _clamp_end()。set_start()/set_end()経由のプログラム的な設定
      （リセット処理・テスト用セットアップ含む）に適用される）
      現在位置 < End に達したら停止 or Start に戻る
      ドラッグ操作限定の追加制約（2026-07-15追加、
      _clamp_start_for_drag()/_clamp_end_for_drag()。
      mouseMoveEvent内でのみ使用）:
        Startは現在地(position_ms)より後ろにはドラッグできない
        Endは現在地(position_ms)より前にはドラッグできない
      この制約をset_start()/set_end()自体（_clamp_start()/
      _clamp_end()）には適用せず、ドラッグ専用メソッドとして分離した
      理由: 動画読み込み直後などposition_msがまだ実際の再生位置と
      同期していないタイミングでset_start()/set_end()が呼ばれる
      ケース（テストでのセットアップ含む）があり、そこに位置制約を
      適用すると意図しない値にクランプされてしまうことが実装時の
      テスト回帰で判明したため。ユーザーが実際にハンドルをドラッグする
      操作（＝mouseMoveEvent経由）にのみ適用することで、要望通り
      「Start/Endを動かす時」の制約として機能させている。

4-5. OffsetWidget (app/offset_widget.py)
  - QWidget を継承
  - ±1秒、±10秒、±1分、±10分 の QPushButton 8個
  - 現在オフセット表示ラベル（例: +00:12 / -01:30）
  - リセットボタンと、その隣に同サイズの強制同期ボタン
    （force_sync_button、QHBoxLayoutで横並び。ラベルは「強制同期」）
    ※ 強制同期ボタン自体はクリックシグナルを外部に公開するのみで、
    オフセット計算ロジックは持たない（GPX/動画の状態を保持しない
    ためMainWindow側で計算する。4-1節参照）
  - シグナル:
      offset_changed(seconds: float)
  - メソッド:
      set_offset(seconds: float)
        現在値に関わらずoffsetを直接上書きし、ラベル更新＋
        offset_changed発火する（既存の各ステップボタンによる
        _adjust()が「加算」なのに対し、こちらは「上書き」）。
        MainWindowのon_force_sync_clicked()から呼ばれる。

4-6. GPXHandler (app/gpx_handler.py)
  - load(path: str) -> gpxpy.GPX
  - from_camm_points(camm_points: list[tuple[int, float, float, float,
    float]]) -> GPXHandler（23章、2026-07-15追加。classmethod）
      camm_encoder.extract_gps_track()の戻り値（(relative_ms,
      latitude, longitude, elevation, epoch_time)のリスト）から、
      実時刻付きのGPXHandlerを構築する。epoch_time
      （datetime.fromtimestamp(epoch_time, tz=UTC)）のみを使い、
      relative_ms（動画自身のタイムライン上の位置）は無視する。
      構築したGPXHandlerは、以降get_all_points()等の既存メソッド
      経由でしか参照されないため、load()で読み込んだ通常のGPXHandler
      と完全に等価に扱える（interpolate_position/has_overlap/
      classify_points_in_range等すべてがそのまま使える）。
  - get_all_points() -> list[GPXTrackPoint]
      GPX内の全トラック・全セグメントのポイントを時刻順にソートし、
      1本の連続した点列として返す（複数トラック/セグメントも結合する）
  - interpolate_position(video_time_ms: int,
                         offset_sec: float,
                         video_creation_time: datetime,
                         video_time_scale: float = 1.0
                        ) -> tuple[float, float] | None
    ロジック:
      gpx_time = video_creation_time
                 + timedelta(milliseconds=
                     playback_ms_to_real_ms(video_time_ms, video_time_scale))
                 + timedelta(seconds=offset_sec)
      gpxの前後のポイントから線形補間で緯度経度を返す
      対応する時刻範囲が記録範囲外の場合は None を返す

      注記: 旧版では引数に video_start_ms を含めていたが、
      6節の数式で video_start_ms が代数的に相殺されることが
      判明したため削除し、代わりに必須である video_creation_time
      を引数に追加した（旧シグネチャには video_creation_time が
      無く、絶対時刻への変換が実装不可能だった）。

      video_time_scale はタイムラプス動画対応（15章）の追加引数。
      playback_ms_to_real_ms()（app/time_utils.py）は
      round(video_time_ms * video_time_scale) を返すだけの単純な
      関数。デフォルト1.0（通常動画）では従来と全く同じ結果になる。
      get_points_for_camm() / _build_output_points() /
      _raw_time_range() / has_overlap() にも同じ video_time_scale
      引数（デフォルト1.0）を追加し、内部で playback_ms_to_real_ms()
      を使って一貫して実世界時刻に変換する（フェーズ2で対応。15章）。

  - get_points_for_camm(video_start_ms: int,
                        video_end_ms: int,
                        offset_sec: float,
                        video_creation_time: datetime,
                        video_time_scale: float = 1.0
                       ) -> list[tuple[int, float, float, float, float]]
    CAMMトラック埋め込み（camm_encoder.py・15章）用に、
    _build_output_points() で得た点列を(relative_ms, latitude,
    longitude, elevation, epoch_time)のタプル列に変換して返す。
    elevationがNoneの場合は0.0を使う。
      true_start_time = video_creation_time
                         + timedelta(milliseconds=
                             playback_ms_to_real_ms(video_start_ms, video_time_scale))
      real_ms_delta = round((true_time - true_start_time).total_seconds() * 1000)
      relative_ms = real_ms_to_playback_ms(real_ms_delta, video_time_scale)
        # = round(real_ms_delta / video_time_scale)
        # 動画自身の圧縮されたネイティブな再生タイムライン上の位置
        # （CAMMサンプルをどのフレームに対応させるかの空間的配置）
      epoch_time = true_time.timestamp()
        # 実際の撮影時刻(Unixエポック秒)。mapillary_tools側が
        # time_gps_epochとして直接採用するため、relative_msが
        # 圧縮されていても正しい実時刻が得られる（15章）
    video_time_scale=1.0（通常動画）では relative_ms = real_ms_delta
    となり、動作は変更前と完全に同じになる。
    対象範囲抽出・境界補間ロジックは、_build_output_points() という
    内部メソッドに集約している（2026-07-19以前は、廃止済みの
    export_trimmed()（バックアップ用GPX単体出力。25章参照）とも
    共通ロジックとして共有していた名残）。

  - _build_output_points(video_start_ms, video_end_ms, offset_sec,
                         video_creation_time, video_time_scale=1.0)
      -> list[tuple[datetime, float, float, float | None]]
    （true_time, latitude, longitude, elevation）のリストを返す
    内部ヘルパー（get_points_for_camm()が使用）。true_time は常に
    実世界の絶対時刻（video_time_scale適用済み）であり、動画自身の
    圧縮タイムライン上の位置ではない点に注意（コンテナ内配置への
    変換はget_points_for_camm側でreal_ms_to_playback_ms()を使って
    行う）。

  - replace_points(points: list[gpxpy.gpx.GPXTrackPoint]) -> None
    マップマッチング（13章・4-11節）完了後、self.gpx を単一トラック・
    単一セグメントの構造に作り直し、渡されたpointsで完全に置き換える。
    既存の複数トラック/セグメント構造は破棄されるが、get_all_points()
    はもともと全トラック・セグメントを時刻順に結合した1本の点列として
    扱う設計であり（4-6節冒頭）、他の全メソッド（interpolate_position/
    has_overlap/get_points_for_camm）はget_all_points()
    経由でしか点列を参照しないため、置き換え後も既存ロジックへの影響は
    ない。MainWindow.load_gpx()（4-1節）からのみ呼ばれる想定。

  - classify_points_in_range(video_start_ms, video_end_ms, offset_sec,
                              video_creation_time, video_time_scale=1.0
                             ) -> list[bool]（20章、2026-07-15追加）
    get_all_points()と同じ順序・点数で、各点が_raw_time_range()の
    範囲内（＝動画のStart/End出力に含まれる範囲）かどうかを返す。
    has_overlap()と同じ_raw_time_range()を再利用しているため、
    video_time_scale・offset_secの扱いも他メソッドと一貫している。
    マップ上のルート線の色分け表示（MapWidget.update_route_ranges()）
    に使う。

  - clip_to_gps_coverage(video_start_ms, video_end_ms, offset_sec,
                          video_creation_time, video_time_scale=1.0
                         ) -> tuple[int, int]（21章、2026-07-15追加）
    _raw_time_range()で得たraw_start/raw_endが、GPXの実記録範囲
    （points[0].time 〜 points[-1].time）をはみ出している場合、その
    はみ出た分だけStart/Endを記録範囲側に切り詰めた(video_start_ms,
    video_end_ms)を返す。完全にカバーされている場合や記録点が無い
    場合は入力をそのまま返す（＝クロップ不要の判定にも使える）。
  - _raw_time_to_video_ms(raw_time, offset_sec, video_creation_time,
                          video_time_scale) -> int
    _raw_time_range()の逆変換。GPXの生の記録時刻から対応する動画
    再生位置(ms)を求める内部ヘルパー（clip_to_gps_coverage()専用）。

4-7. VideoHandler (app/video_handler.py)
  - get_metadata(path: str) -> dict
    ffprobe で動画長・作成日時を取得:
      subprocess.run([
          "ffprobe", "-v", "quiet",
          "-print_format", "json",
          "-show_format", "-show_streams",
          path
      ], capture_output=True)
  - get_keyframe_timestamps(path: str) -> list[float]
    ffprobe でキーフレーム位置（秒）の一覧を取得:
      subprocess.run([
          "ffprobe", "-v", "quiet",
          "-select_streams", "v:0",
          "-skip_frame", "nokey",
          "-show_entries", "frame=pts_time",
          "-of", "csv=print_section=0",
          path
      ], capture_output=True)
    注: 実機検証で、出力の1行目のみ末尾にカンマが付く場合がある
    （例: "0.000000,"）ため、パース時は各行を","で分割し
    先頭要素のみ使う実装にする。
  - get_fps(path: str) -> float
    ffprobeのr_frame_rate（例: "30000/1001"）から算出:
      subprocess.run([
          "ffprobe", "-v", "quiet",
          "-select_streams", "v:0",
          "-show_entries", "stream=r_frame_rate",
          "-of", "csv=p=0",
          path
      ], capture_output=True)
      → "num/den" 形式の文字列をfloatに変換する
  - has_audio_stream(path: str) -> bool（15章。タイムラプスの
    可能性を示す弱いヒント判定に使用）
      subprocess.run([
          "ffprobe", "-v", "quiet",
          "-select_streams", "a",
          "-show_entries", "stream=index",
          "-of", "csv=p=0",
          path
      ], capture_output=True)
      → 標準出力が空でなければ音声ストリームありと判定する
  - export_trimmed(input_path: str,
                   output_path: str,
                   start_ms: int,
                   end_ms: int,
                   video_creation_time: datetime | None = None)
    Smart Cut方式（境界GOPのみ再エンコード＋中間はストリームコピー）:
      1. get_keyframe_timestamps() でキーフレーム位置を取得
      2. start_ms以降で最初のキーフレーム時刻 kf_start を求める
         （start_msがちょうどキーフレームに一致する場合は区間Aは不要）
      3. end_ms以前で最後のキーフレーム時刻 kf_end を求める
         （end_msがちょうどキーフレームに一致する場合は区間Cは不要）
      4. kf_start >= kf_end の場合（Start/Endが同一GOP内に収まる場合）:
         区間分割はせず、start_ms〜end_msを単一の再エンコードで
         書き出して終了する（区間B自体が存在しないため）
      5. 区間A（再エンコード）: start_ms 〜 kf_start
           subprocess.run([
               "ffmpeg", "-ss", str(start_ms/1000), "-to", str(kf_start),
               "-i", input_path,
               "-c:v", "libx264", "-preset", "fast", "-crf", "18",
               "-an",
               tmp_part_a
           ])
      6. 区間B（ストリームコピー）: kf_start 〜 kf_end
         注意: "-to" によるストリームコピーの終端指定は、
         B-フレームの並び替え（DTS/PTSのずれ）により、意図した
         キーフレーム境界を2〜4フレーム超過する場合があることを
         実機検証で確認した（ffmpeg 8.1.2で実測: 期待144フレームに
         対し146フレーム混入）。これを避けるため、"-to" ではなく
         正確なフレーム数指定（-frames:v）を用いる:
           fps = get_fps(input_path)  # 例: r_frame_rate から算出
           frame_count = round((kf_end - kf_start) * fps)
           subprocess.run([
               "ffmpeg", "-ss", str(kf_start),
               "-i", input_path,
               "-frames:v", str(frame_count),
               "-c", "copy",
               "-an",
               tmp_part_b
           ])
      7. 区間C（再エンコード）: kf_end 〜 end_ms
           subprocess.run([
               "ffmpeg", "-ss", str(kf_end), "-to", str(end_ms/1000),
               "-i", input_path,
               "-c:v", "libx264", "-preset", "fast", "-crf", "18",
               "-an",
               tmp_part_c
           ])
      8. concat demuxer で区間A+B+Cを結合:
           concat_list.txt に各ファイルパスを記載し
           subprocess.run([
               "ffmpeg", "-f", "concat", "-safe", "0",
               "-i", concat_list_path,
               "-c", "copy",
               output_path
           ])
      9. 一時ファイル（tmp_part_a/b/c, concat_list.txt）を削除

      注: 区間A・区間Cの再エンコード設定（コーデック・crf等）は
      入力動画のコーデックに応じて調整する。存在しない区間
      （境界がキーフレームに一致する場合）はスキップして
      concatの対象から除外する。

      音声について（2026-07-18改訂。当初は「実害なし」と判断していたが
      誤りだったことが判明）:
      当初は各区間の再エンコード境界でAACエンコーダのpriming/padding
      sampleにより、コンテナ全体の再生時間が数十ミリ秒（実測23ms程度）
      ずれる場合があるが、映像フレーム数は正確に一致するため実用上
      問題ないと判断していた。しかし2026-07-18、ユーザー自身が過去に
      本ツールでExportした動画（タイムラプス設定、実測で音声トラック
      のみ映像トラックより約53ms長い）を読み込んで再度Exportしようと
      した際、GPS未カバー区間として誤って検出され「動画をクロップ
      します」という警告が出る不具合として顕在化した。原因は、
      VideoHandler.get_duration_ms()が読むコンテナ全体の長さ
      （ffprobeのformat.duration）が、複数トラックのうち最長の
      トラック（音声）に引きずられて決まるため、動画タイムラインの
      Endが実際の映像・GPSデータの終端より音声の分だけ長く表示されて
      しまい、5-10節の判定ロジックがその差分をGPS未カバーと誤認する
      ことだった。
      根本的に、mapillary_tools向けのMapillary投稿用途では音声は
      不要であるため、そもそも出力に音声を含めない方針に変更した
      （_reencode_segment()・_copy_segment_by_frames()いずれも
      -c:a aac / -c copy(音声含む)を-anに変更し、音声トラック自体を
      出力しないようにした）。これにより長さのズレが発生する余地が
      なくなり、副次的に上記の誤クロップ検出も解消される。
      mapillary_tools（0.14.7）が音声なしのCAMM埋め込み動画を問題なく
      処理できることは実機で確認済み（tests/test_mapillary_validator.py
      と同じ手法で、音声を除去したテスト動画に対しvalidate_export()を
      実行し、画像件数・エラー無しを確認）。

      重要（Step 8のmapillary_tools連携検証で判明した問題と対処）:
      Smart Cutで生成したconcat出力には、元動画が持っていた
      creation_timeメタデータが一切引き継がれないことを実機で確認した
      （ffprobeで空になる）。対処として、video_creation_time が
      渡された場合は
      new_creation_time = video_creation_time + timedelta(milliseconds=start_ms)
      を計算し、最終出力（concatステップ、または単一GOP内で完結する
      再エンコードのケース）に
        "-metadata", f"creation_time={new_creation_time.isoformat()}"
      を付与するようにした。
      ただし4-8節（CammEncoder）で後述する通り、この直後にMP4Boxで
      CAMMトラックを追加する際、MP4Box自身がcreation_timeを
      「処理を実行した現在時刻」で上書きしてしまうことを実機で確認した。
      そのため、最終出力ファイルのcreation_timeメタデータは正確な値に
      ならない前提とし、mapillary_toolsへは10節の
      --video_start_time オプション（コマンド実行時にミリ秒精度で
      明示指定）で正しい時刻を渡す設計とする。ここでのffmpeg側の
      creation_time設定は「他ツールで開いた際のベストエフォートな
      参考値」の位置づけとする。

4-8. CammEncoder (app/camm_encoder.py)
  Smart Cutで生成した動画に、GPXから計算したGPS位置情報を
  Google CAMM（Camera Motion Metadata）形式の埋め込みメタデータ
  トラックとして追加する。GPAC付属の MP4Box を subprocess 経由で
  呼び出して実現する。

  背景・実機検証で判明したこと:
  - CAMMはGoogleの公開仕様（developers.google.com/streetview/publish/camm-spec）。
    当初はGPSサンプル（Type 5, MIN_GPS）: reserved(uint16) +
    type(uint16)=5 + latitude(double) + longitude(double) +
    altitude(double) の28バイトを使っていたが、15章のタイムラプス
    対応（フェーズ2）で **Type 6（GPS）** に変更した。Type 6は
    time_gps_epoch（UTCエポック秒での実際の撮影時刻）を含む、より
    情報量の多いフォーマットで、mapillary_tools側がこれを検出できれば
    動画自体のfps・PTSと無関係に正しい実撮影時刻を採用してくれる
    （15章で詳述）。
  - ffmpeg自体には任意の4CC（'camm'）でトラックを書き込む機能がないが、
    GPACのMP4Boxには「NHML」という汎用の生サンプルインポート形式があり、
    mediaSubType="camm" を指定することで、未知コーデック用の
    汎用サンプルエントリとして書き込めることを実機で確認した。
  - mapillary_tools（0.14.7）は --geotag_source exif（10章参照。
    camm ではない点に注意）で、このType 6 CAMMトラックから
    サンプリング段階（sample_video）が書き込んだ画像EXIFの位置・
    time_gps_epoch由来の実時刻を正しく読み取り、ジオタグできることを
    実機で確認済み（座標・実時刻が期待値と完全一致。15章）。
  - 制約1（解消済み）: 旧Type 5実装では
    --video_sample_distance（距離ベースサンプリング）でのGPS自動検出が
    「No GPS data found from the video」で失敗し、時間ベース
    （--video_sample_distance -1 --video_sample_interval N）を
    使わざるを得なかった。Type 6への切り替え後、距離ベースサンプリングが
    正常に動作することを実機で確認した（15章）。時間ベースサンプリングは
    CAMMのtime_gps_epochを一切参照しない別のコードパスであることが
    判明したため、現在は距離ベースサンプリングを採用している（10章）。
  - 制約2: ffmpegは未知コーデックのトラックをストリームコピーで
    パススルーできない（"Could not find tag for codec none in
    stream" エラーで失敗することを実機確認）。そのため
    CAMMトラック埋め込み（MP4Box）は、ffmpegによる処理
    （Smart Cut、creation_time設定）が全て完了した後の
    **最後の工程**として実行し、それ以降はffmpegで触らないこと。
  - 制約3: MP4Boxでトラックを追加すると、動画のcreation_time
    メタデータが「処理を実行した現在時刻」で上書きされることを
    実機確認した。この対策として、mapillary_toolsには
    --video_start_time をコマンド引数として明示指定する
    （ファイル内のcreation_timeに依存しない）。実機検証で
    --video_start_time を指定すれば、creation_timeが不正な値でも
    正しくジオタグできることを確認済み。

  - CAMM_GPS_TYPE6_STRUCT = struct.Struct("<HHdiddfffffff")
    （リトルエンディアン。フィールド順:
     reserved(uint16), type(uint16)=6, time_gps_epoch(double),
     gps_fix_type(int32), latitude(double), longitude(double),
     altitude(float), horizontal_accuracy(float),
     vertical_accuracy(float), velocity_east/north/up(float×3),
     speed_accuracy(float)。合計60バイト）
    mapillary_tools側の camm/camm_parser.py の GPSSampleEntry.construct
    と完全に一致するフィールド順・型で実装した（実機検証で
    time_gps_epochが正しく読み取れることを確認済み）。
    速度系フィールド(velocity_*)は本ツールでは計算せず0.0固定、
    精度系フィールド(horizontal_accuracy/vertical_accuracy/
    speed_accuracy)は実機検証で問題が出なかった無難な値
    （5.0/5.0/1.0）を固定で使う。gps_fix_typeは3（3D fix）固定。

  - embed_gps_track(video_path: str,
                    output_path: str,
                    points: list[tuple[int, float, float, float, float]])
    points は (relative_ms, latitude, longitude, elevation, epoch_time)
    のリスト（GPXHandler.get_points_for_camm() の戻り値をそのまま渡す）。
    relative_ms は動画自身の圧縮されたネイティブな再生タイムライン上の
    位置（CAMMサンプルをどのフレームに対応させるかの空間的な配置）、
    epoch_time はそのGPS点の実際の撮影時刻（Unixエポック秒、15章）。
    ロジック:
      1. pointsをCAMM Type 6バイナリ（各60バイト、リトルエンディアン）
         にエンコードし、一時的な .raw ファイルに書き出す
           CAMM_GPS_TYPE6_STRUCT.pack(
               0, 6, epoch_time, 3, lat, lon, elevation or 0.0,
               5.0, 5.0, 0.0, 0.0, 0.0, 1.0)
      2. NHMLマニフェスト（.nhml、XML）を生成:
           <NHNTStream mediaType="meta" mediaSubType="camm"
                       codecID="camm" timeScale="1000"
                       baseMediaFile="<rawファイル名>">
             <NHNTSample DTS="<relative_ms>" dataLength="60"
                         mediaOffset="<累積バイトオフセット>"/>
             ...
           </NHNTStream>
      3. MP4Boxを実行して既存動画にトラックを追加:
           subprocess.run([
               "MP4Box", "-add", video_path,
               "-add", nhml_path,
               output_path
           ])
      4. 一時ファイル（.raw, .nhml）を削除

  - extract_gps_track(video_path: str)
      -> list[tuple[int, float, float, float, float]]
    embed_gps_track() の逆操作。戻り値はembed_gps_track()の入力と
    対称な(relative_ms, latitude, longitude, elevation, epoch_time)
    の5要素タプルのリスト（2026-07-15変更。以前は検証モード
    VerificationWindow専用でtime_gps_epoch等を読み捨てた4要素
    タプルだったが、23章でCAMM埋め込みGPSをGPXHandler相当のデータ
    として取り込む（GPXHandler.from_camm_points()、4-6節）ために
    epoch_timeが必要になったため、読み捨てるのをやめた）。
    実機検証で embed → extract の往復が5フィールドすべてについて
    完全に一致することを確認済み（tests/test_camm_encoder.py
    test_embed_then_extract_round_trip）。
    ロジック:
      1. subprocess.run(["MP4Box", "-info", video_path]) の出力を
         パースし、"Media Type: meta:camm" を含むトラックのIDを特定
         （見つからない場合は CammTrackNotFoundError を送出）
      2. 一時ディレクトリで以下を実行し、.nhml と .media
         （生バイナリ）のペアを書き出す:
           subprocess.run(["MP4Box", "-nhml", str(track_id), video_path])
      3. .nhml（XML）をパースし、各 NHNTSample の DTS(relative_ms)・
         dataLength・累積バイトオフセットを取得
      4. .media ファイルから該当バイト列を読み、
         CAMM_GPS_TYPE6_STRUCT.unpack(chunk) でデコードして
         (relative_ms, latitude, longitude, elevation, time_gps_epoch)
         を得る（gps_fix_type・各種accuracy・velocityは引き続き破棄）
      5. 一時ファイル（.nhml, .media）を削除

  - interpolate_camm_points()は2026-07-15に削除した（23章）。
    検証モード（VerificationWindow）専用のヘルパーだったが、
    検証モード廃止に伴い呼び出し元が無くなったため。動画埋め込み
    GPSをGPXデータとして扱う経路（GPXHandler.from_camm_points()）
    では、通常のGPXHandler.interpolate_position()
    （二分探索＋線形補間、絶対時刻ベース）がそのまま使われる。

4-9. Exporter (app/exporter.py)
  - default_video_filename(state: AppState) -> str（22章、
    2026-07-15追加）
      保存ダイアログ（QFileDialog.getSaveFileName）に初期表示する
      動画ファイル名を返す: f"{元動画ファイル名（拡張子除く）}_synced.mp4"
      （連番は付与しない。保存ダイアログ自体がOS標準の上書き確認を
      行うため）

  - export(state: AppState, video_output_path: str) -> str（22章、
    2026-07-15変更: 第2引数が output_dir から video_output_path に
    変更された。2026-07-19変更: 戻り値がtuple[str, str]（動画・GPX
    パス）からstr（動画パスのみ）に変更。GPX単体出力機能を廃止した
    ため。25章参照）
    1. GPXとオフセット適用後の時刻範囲が動画Start〜Endと重複するか
       事前チェック（重複しない場合はこの関数を呼び出せない
       ＝出力ボタンが無効化されている想定）
    2. VideoHandler.export_trimmed() を呼び出し、一時パスに
       Smart Cut済み動画（creation_time設定込み）を書き出す
    3. GPXHandler.get_points_for_camm() でCAMM用の点列を取得し、
       CammEncoder.embed_gps_track() を呼び出して、2.の一時動画に
       CAMMトラックを追加した最終版を video_output_path の場所に
       書き出す（2.の一時ファイルは削除する）。動画側は
       video_output_path をそのまま使う（保存ダイアログでユーザーが
       明示的に指定した値であり、連番付与は行わない）
    ※ 完了ダイアログの表示・mapillary_toolsコマンド例の生成・14章の
    ローカル検証の起動は Exporter.export() の責務ではなく、
    MainWindow.on_export_clicked()（4-1節）が export() 完了後に
    呼び出す形で行う（Exporterはファイル生成のみに責務を絞る）

  - can_export(state: AppState) -> bool
      GPXとオフセット適用後の時刻範囲が動画Start〜Endと
      重複するかどうかを判定し、出力ボタンの有効/無効に反映する。
      内部で GPXHandler.has_overlap() に state.video_time_scale を
      渡すため、タイムラプス設定（15章）が有効な場合も正しい実時刻
      範囲で判定される。

  - get_video_start_time_str(state: AppState) -> str
      トリミング後の動画の実際の録画開始時刻を、mapillary_toolsの
      --video_start_time 形式（YYYY_MM_DD_HH_MM_SS_sss、UTC）で返す。
      build_mapillary_tools_command() と MapillaryValidator（4-13節）
      の両方から使う共通値。
        synced_start_time = state.video_creation_time
                             + timedelta(milliseconds=
                                 playback_ms_to_real_ms(
                                     state.video_start_ms, state.video_time_scale))
      video_time_scale=1.0（通常動画）では従来と同じ計算になる。

4-10. （廃止: 旧VerificationWindow。23章参照）
  app/verification_window.py・tests/test_verification_window.pyは
  2026-07-15に削除した。動画に埋め込まれたCAMM GPSトラックを
  目視確認する機能は、通常モード（MainWindow）に統合されたため、
  独立ウィンドウとしては不要になった（23章の背景・設計を参照）。
  以降の4-11節以降の番号は振り直していない（本ドキュメントの既存の
  節番号への参照を維持するため）。

4-11. MapMatcher (app/map_matcher.py)
  GPX読み込み時（5-9節）に、記録軌跡を道路網へスナップする。
  設計・アルゴリズムは姉妹プロジェクト ../gpx-editor（React/JS版、
  src/lib/mapMatching.js）に倣い、Pythonへ移植する。
  gpx-editorとの違いは、gpx-vsyncでは各点の記録時刻・標高を
  スナップ後も保持する必要がある点（時刻同期の根幹機能のため）。
  外部API仕様の詳細は13章を参照。

  Douglas-Peucker間引き廃止について（2026-07-14、フェーズ変更）:
    当初はgpx-editorのrdp.jsに倣い、送信前にDouglas-Peucker法
    （RDP_EPSILON=0.00005度≒5.5m）で軌跡点を間引いていた。しかし
    実際のユーザーデータ（1Hzロガーで記録された382点のGPX）で
    検証したところ、間引き後は26点まで減少（93%削減）しており、
    トラックポイントの記録間隔がそもそも0.5〜1秒程度と細かい
    ケースでは、動画フレームとの時刻補間の分解能を不必要に
    大きく犠牲にしていることが判明した。ユーザーからのフィードバック
    （「間引きすぎ」「trkpt間隔が0.5秒より長いなら間引き無しに
    してほしい」）を受け、間引きを完全に廃止し、全トラックポイントを
    そのままマップマッチングAPIに送信する方式に変更した
    （rdp_mask/rdp_simplify/RDP_EPSILONは削除済み）。

  座標のみを扱う純粋ロジック（gpx-editorのmapMatching.jsに対応、
  gpxpy非依存でユニットテストしやすい層）:

  - CHUNK_SIZE = 50（gpx-editorと同一値）
  - MIN_REQUEST_INTERVAL_SEC = 1.0（fair-use対応。下記match_route参照）

  - match_chunk(chunk: list[tuple[float, float]],
               search_radius: float = 50, timeout: float = 30
              ) -> dict
    Valhalla trace_attributes（13章）へのPOSTリクエストを1回行い、
    レスポンスJSONをそのまま返す。costing=bicycle固定、
    shape_match=map_snap固定。requestsライブラリのtimeout引数で
    タイムアウトを指定する（AbortController相当）。
    X-Client-Id: gpx-vsync ヘッダーを付与する（13章）。
    HTTPエラー・タイムアウト時は例外を送出する（呼び出し側の
    match_route()がチャンク単位で継続/中断を判断する。
    gpx-editorのmatchChunk()と同じ責務分担）。

  - apply_matched_points(original_chunk, match_response) -> list[tuple[float, float]]
    match_responseの matched_points 配列を見て、type が
    "matched" または "interpolated" の点だけ座標を置き換える。
    それ以外（unmatched等）は元座標を保持する。
    gpx-editorのapplyMatchedPoints()と同一ロジック。

  - @dataclass MatchResult:
      matched_points: list[tuple[float, float]]
      n_snapped: int
      status: str  # "完了" | "エラー" | "キャンセル"
      error: str | None

  - match_route(points: list[tuple[float, float]],
               on_progress: Callable[[MatchProgress], None] | None = None,
               should_cancel: Callable[[], bool] | None = None,
               match_chunk_impl: Callable = match_chunk,
               sleep_impl: Callable[[float], None] = time.sleep
              ) -> MatchResult
    points を50点ずつのチャンクに分割し、チャンクごとに
    match_chunk_impl() を順次呼び出す（gpx-editorのmatchRoute()と
    同一ロジック）。
      - 各チャンク処理前に should_cancel() を確認し、Trueなら
        その時点までの結果で status="キャンセル" として返す
      - fair-use対応（2026-07-14追加）: 2チャンク目以降は、送信前に
        直前チャンクの送信開始時刻からMIN_REQUEST_INTERVAL_SEC
        （1.0秒）経過しているか確認し、経過していなければ不足分だけ
        sleep_impl()で待機してから送信する。match_chunk_impl自体の
        応答時間（ネットワーク遅延込み）は待機時間から差し引かれる
        ため、レスポンスが遅い場合に余分な待機は発生しない。
        1チャンクのみで完結する場合は待機自体が発生しない。
        sleep_impl はテスト時に高速なダミー（例:
        `lambda _seconds: None`）へ差し替えられるようDI可能にした
        （実運用では既定のtime.sleepを使う）
      - チャンク0（1チャンク目）で例外発生: 全体を自動キャンセル扱いにし、
        元のpointsをそのまま status="キャンセル"、
        error="1チャンク目タイムアウトにより自動キャンセル" として返す
      - チャンク1以降で例外発生: そのチャンクは元座標を保持し、
        エラーメッセージを蓄積して処理を継続する
      - 全チャンク完了時: n_snapped > 0 なら status="完了"、
        0件なら status="エラー"
      - on_progress は各チャンク処理後に
        {chunk_idx, total_chunks, n_snapped, status, error} 相当の
        情報で呼び出す（MapMatchingWorkerの進捗シグナル用。4-12節）

  gpxpy連携アダプタ（時刻・標高を保持する層。gpx-vsync固有）:

  - @dataclass GpxMatchResult:
      points: list[gpxpy.gpx.GPXTrackPoint]
      n_snapped: int
      status: str
      error: str | None

  - match_gpx_points(points: list[gpxpy.gpx.GPXTrackPoint],
                     on_progress=None, should_cancel=None,
                     match_chunk_impl=match_chunk,
                     sleep_impl=time.sleep
                    ) -> GpxMatchResult
    1. coords = [(p.latitude, p.longitude) for p in points]（間引きなし）
    2. result = match_route(coords, on_progress=on_progress,
                            should_cancel=should_cancel,
                            match_chunk_impl=match_chunk_impl,
                            sleep_impl=sleep_impl)
    3. points と result.matched_points を1:1でzipし、各点の
       time/elevationは元のpointsから引き継いだ新しいGPXTrackPointの
       リストを構築して返す（間引きが無いため、インデックスの
       付け替え（旧kept_indices）は不要）
       （座標のみ置き換え、時刻・標高は無変更）

4-12. MapMatchingWorker / MapMatchingDialog
      (app/map_matching_worker.py, app/map_matching_dialog.py)
  GPX読み込み中にネットワークI/Oでメインスレッド（GUIイベントループ）を
  ブロックしないためのQThreadラッパーと、進捗・キャンセルUI。
  gpx-editorのuseMapMatching.js（React状態管理ラッパー）+
  MapMatchingDialog.jsxに相当する。

  - class MapMatchingWorker(QThread)
      シグナル:
        progress = pyqtSignal(int, int)  # chunk_idx, total_chunks
        finished_matching = pyqtSignal(object)  # GpxMatchResult
      コンストラクタ: MapMatchingWorker(points: list[GPXTrackPoint])
      run(): map_matcher.match_gpx_points() を
        on_progress=(進捗シグナルemit)、
        should_cancel=(内部の threading.Event / bool フラグ参照)
        で呼び出し、完了後 finished_matching をemitする
      request_cancel(): 内部キャンセルフラグをTrueにする
        （gpx-editorのuseMapMatching.jsのcancelRequestedRefと同じ
        役割。次のチャンク処理前チェックで反映される）

  - class MapMatchingDialog(QDialog)
      モーダルダイアログ。「マップマッチング中」の見出し、
      プログレスバー（chunk_idx/total_chunks）、
      「キャンセル」ボタン（クリックでworker.request_cancel()を呼ぶ）
      worker.progress シグナルに接続してプログレスバーを更新する
      worker.finished_matching シグナルに接続して自動的に閉じる
      （MainWindow.load_gpx()がこのシグナルで結果を受け取り、
      GPXHandler.replace_points()を呼ぶ。4-1節）

4-13. MapillaryValidator (app/mapillary_validator.py)
  出力完了直後、生成された動画から実際にmapillary_toolsがGPS付き
  画像相当データを取り出せるかをローカルで検証する（5-10節）。
  mapillary_toolsのサーバーへは一切データを送信しない
  （`video_process` はローカル処理のみのサブコマンド。10章参照）。

  背景・実機検証で判明したこと（2026-07-13、mapillary_tools 0.14.7）:
  - `video_process` の第2位置引数（import_path）でサンプル画像の
    出力先ディレクトリを明示指定できる（省略時は
    `{動画のディレクトリ}/mapillary_sampled_video_frames`）。これを
    一時ディレクトリへ向けることで、出力動画のあるユーザーの
    ディレクトリには一切副産物ファイルが生成されないことを実機で
    確認済み（video_import_pathには最終出力動画のパスをそのまま
    渡せばよく、コピーは不要）。
  - GPSデータが全く取得できない場合（例: CAMM未埋め込み動画）は
    終了コード3、desc.jsonファイル自体が生成されないことを実機で
    確認済み。標準エラー出力に
    "WARNING - No GPS data found from the video" が出る。
  - 正常時は終了コード0、指定した --desc_path にJSON配列が
    書き出される。各要素は MAPLatitude/MAPLongitude/
    MAPCaptureTime/MAPFilename 等を持つ（実機出力で確認済み）。
  - ログ行は `HH:MM:SS.mmm - <LEVEL> - <message>` 形式
    （LEVELは INFO/WARNING/ERROR など、桁揃えのため空白パディング
    あり）。ERROR/WARNING行の抽出には正規表現
    `r"-\s*ERROR\s*-"` / `r"-\s*WARNING\s*-"` を用いる。

  - is_mapillary_tools_available() -> bool
      shutil.which("mapillary_tools") is not None

  - @dataclass ValidationResult:
      ok: bool
      n_images: int
      errors: list[str]
      warnings: list[str]

  - validate_export(video_path: str, video_start_time: str,
                    should_cancel: Callable[[], bool] | None = None
                   ) -> ValidationResult
    1. tempfile.TemporaryDirectory() 内に samples_dir・desc_path を
       用意する
    2. subprocess.Popen で以下を実行する（Popenを使うのは
       should_cancel() による途中終了(terminate())に対応するため）:
         mapillary_tools video_process <video_path> <samples_dir>
             --geotag_source exif
             --video_start_time <video_start_time>
             --video_sample_distance 3
             --video_sample_interval -1
             --filetypes image
             --desc_path <desc_path>
       --filetypes imageは24章で追加（2026-07-18。mapillary_tools自身が
       「無いと動画サンプルと動画ファイル自体の両方を処理・アップロード
       してしまう可能性がある」と警告するため付与）
       短い間隔でプロセスの生死をポーリングしつつ should_cancel() を
       確認し、Trueならprocess.terminate()した上で
       ValidationResult(ok=False, n_images=0, errors=[],
       warnings=[], ※cancelled扱いは呼び出し側のstatusで区別)を返す
    3. 標準エラー出力をログ行ごとに走査し、ERROR/WARNING行を
       それぞれ errors/warnings リストに集める
    4. 終了コードが0以外、または desc_path が存在しない場合:
       ok=False（errorsが空ならstderr全体を1件として格納する）
    5. desc_path を読み込み、JSON配列の要素数を n_images とする。
       n_images == 0 の場合も ok=False とする
    6. 一時ディレクトリ（samples_dir・desc_path含む）は
       with文終了時に自動削除される

4-14. MapillaryValidationWorker / MapillaryValidationDialog
      (app/mapillary_validation_worker.py,
       app/mapillary_validation_dialog.py)
  4-12節のMapMatchingWorker/MapMatchingDialogと同一パターン
  （QThread + モーダルダイアログでのブロッキング待機）。

  - class MapillaryValidationWorker(QThread)
      シグナル:
        finished_validation = pyqtSignal(object)  # ValidationResult | None
        （Noneはキャンセルを表す）
      コンストラクタ: MapillaryValidationWorker(video_path: str,
                                              video_start_time: str)
      run(): mapillary_validator.validate_export() を
        should_cancel=(内部の threading.Event 参照) で呼び出し、
        完了後 finished_validation をemitする
      request_cancel(): 内部キャンセルフラグをTrueにする

  - class MapillaryValidationDialog(QDialog)
      モーダルダイアログ。「ローカル検証中」の見出し、
      不定形（busy）プログレスバー（QProgressBar.setRange(0, 0)。
      mapillary_tools側から細かい進捗が取得できないため）、
      「キャンセル」ボタン（クリックでworker.request_cancel()を呼ぶ）
      worker.finished_validation シグナルに接続して自動的に閉じる

4-15. time_utils (app/time_utils.py)
  - モジュール関数のみ、クラスは持たない
  - playback_ms_to_real_ms(ms: int, time_scale: float) -> int
      return round(ms * time_scale)
    動画の再生位置(ms)を実世界での経過時間(ms)に変換する単純な純関数。
    GPXHandler.interpolate_position()・MainWindow.on_force_sync_clicked()・
    Exporter.get_video_start_time_str() から使う共通ロジック（15章）。
  - real_ms_to_playback_ms(real_ms: int, time_scale: float) -> int
      return round(real_ms / time_scale)
    playback_ms_to_real_ms()の逆変換。実世界での経過時間(ms)を、動画
    自身の圧縮されたネイティブな再生タイムライン上の位置(ms)に戻す。
    GPXHandler.get_points_for_camm()がCAMMサンプルの配置位置
    （relative_ms）を計算する際に使う（15章フェーズ2）。

4-16. TimelapseWidget (app/timelapse_widget.py)
  - QWidget を継承
  - 配置: MainWindow右ペイン、VideoWidget（タイムライン含む）の下
  - 構成要素:
      QCheckBox      # 「タイムラプス動画」チェックボックス（既定OFF）
      QDoubleSpinBox # 間隔入力欄（秒。範囲0.01〜3600.0、既定0.5、
                     # 小数点2桁、チェックがONの時のみ有効化）
  - シグナル:
      timelapse_changed(enabled: bool, interval_sec: float)
        チェックボックスのtoggle時、および有効化中の間隔値変更時に発火
  - メソッド:
      timelapse_enabled() -> bool
      interval_seconds() -> float
      reset() -> None
        動画差し替え時にMainWindow.load_video()から呼ばれる。
        チェックボックスOFF・間隔を既定値(0.5)に戻し、
        timelapse_changed(False, 0.5) を1回だけ発火する。
        内部実装注記: チェックボックスと間隔入力を個別に
        setChecked()/setValue()すると、両方がシグナル発火対象になり
        中間状態（例: enabled=True, interval=0.5 という到達しない
        はずの組み合わせ）が一瞬発火してしまう不具合を実装時に
        確認した。そのため reset() 内では両ウィジェットの
        blockSignals(True) で一時的にシグナルを止めて最終状態に
        更新し、その後に timelapse_changed を手動で1回だけ発火する
        実装とする。


4-17. theme (app/theme.py) — 16章・17章・18章
  - アプリ全体の配色・QSSを定義するモジュール（ウィジェットクラスではない）
  - Palette（dataclass）: accent / accent_hover / accent_bg / accent_border /
    bg / card_bg / border / text / text_subtle / warn / error の
    11トークン（warn/errorは17章のマップマッチング結果表示で使用）
  - LIGHT / DARK: それぞれのパレット定数（16章参照）
  - CHECKMARK_ICON_PATH: assets/icons/check.svg（18章）への絶対パス
    （__file__基準で解決）
  - build_stylesheet(p: Palette) -> str
      パレットからQSS文字列を組み立てる（QMainWindow/QWidget、
      QFrame#card、QPushButton（通常/#primaryButton/:hover/:disabled）、
      QLabel[subtle="true"]/[state="ok"|"warn"|"error"]、
      QCheckBox::indicator:checked（CHECKMARK_ICON_PATHを
      image: url()で指定。18章）、
      QDoubleSpinBox/QDateTimeEdit/QLineEdit、
      QSplitter::handle、QMenuBar/QMenu、QDialogButtonBoxが対象）
  - resolve_palette(app: QApplication) -> Palette
      QApplication.styleHints().colorScheme() でmacOSのシステム外観
      （ライト/ダーク）を判定する。offscreenプラットフォーム
      （テスト実行時）ではQt.ColorScheme.Unknownが返ることを実機
      確認済みのため、Dark以外はすべてLightにフォールバックする
  - apply_theme(app: QApplication) -> None
      resolve_palette() + build_stylesheet() の結果を
      app.setStyleSheet() で適用する。main.py からQApplication生成
      直後に1回呼び出す


5. 動画とマップの連携ロジック
----------------------------------------------------------------

  QMediaPlayer.positionChanged シグナルを使い、
  再生位置（ミリ秒）が変わるたびに以下を実行する:

    def on_position_changed(self, ms: int):
        # Start/End 範囲チェック
        if ms >= self.state.video_end_ms:
            self.player.pause()
            self.player.setPosition(self.state.video_end_ms)
            return
        # マップマーカー更新（video_time_scaleはタイムラプス対応。15章。
        # 通常動画では1.0のままなので従来と同じ挙動になる）
        pos = self.gpx_handler.interpolate_position(
            ms,
            self.state.offset_seconds,
            self.state.video_creation_time,
            self.state.video_time_scale
        )
        if pos:
            self.map_widget.update_marker(*pos)
        else:
            # GPXの記録範囲外の場合はマーカーを非表示にする
            self.map_widget.hide_marker()
        # タイムライン更新
        self.video_widget.timeline.set_position(ms)

  更新頻度: 実機検証（PyQt6 6.11.0 / Qt 6.11.1、macOS）では positionChanged は
  数十ms間隔（実測30〜70ms程度）で頻繁に発火することを確認した
  （旧版の「約200ms間隔」という記述は不正確だったため訂正）。
  マップのJS呼び出しはその都度行ってもよいが、頻度が高いため、
  必要であれば間引き（例: 前回呼び出しから100ms未満ならスキップ）を
  検討してもよい。
  2026-07-19追記: PySide6移行（Step29-4）後、実サンプル動画
  （tests/fixtures/sample.mp4）を使った既存の自動テスト一式が
  全てグリーンのままであることを確認済み。上記の実測値そのものを
  PySide6環境で再測定する専用の手動検証は未実施のため、フェーズ2の
  地図実装変更時に併せて再確認する。


6. 動画開始時刻の扱い
----------------------------------------------------------------

  GPXと動画を紐付けるために「動画の録画開始時刻（絶対時刻）」が必要。
  なお、GPXは動画より先に読み込まれていることが前提（4-1参照）なので、
  以下のいずれの場合でも gpx_data は必ず利用可能である。

  取得方法（優先順）:
  1. ffprobeのメタデータから取得
       format.tags.creation_time (ISO8601, UTC)
  2. メタデータがない場合: UIに入力フォームを表示し手動入力させる
       - 入力値はローカル時刻として扱う
       - 入力欄の初期値には、GPXの最初のトラックポイント時刻
         （UTC）をシステムのタイムゾーンでローカル時刻に変換した値を
         プリフィルする
       - ユーザーはこの初期値を確認・修正した上で確定する
       - 確定後、システムのタイムゾーンを使ってUTCに変換し、
         内部的にはUTCの video_creation_time として保持する

  動画上の任意の再生位置（ms）に対応するGPX時刻:
    gpx_time = video_creation_time
               + timedelta(milliseconds=video_start_ms)  ← Startマーカー分
               + timedelta(milliseconds=current_pos_ms - video_start_ms)
               + timedelta(seconds=offset_seconds)


7. Startマーカー・Endマーカーの実装詳細
----------------------------------------------------------------

  CustomTimeline の paintEvent:
    - バー全体をグレーで描画
    - Start〜End 区間をハイライト（青系）
    - 現在位置を赤い縦線で描画
    - Start: 緑の下向き三角ハンドル
    - End:   赤の下向き三角ハンドル

  ドラッグ判定:
    マウスダウン時に Start/End ハンドルとの距離を判定し、
    近い方をドラッグ対象にする（10px以内など）

  制約チェック:
    - Start < End（逆転不可）
    - Start >= 0
    - End <= duration

  End到達時の挙動:
    - 現在位置がEndに達したら一時停止し、End位置に固定する
      （Startには戻らない。ループ再生は行わない）


8. レイアウト詳細
----------------------------------------------------------------

  QMainWindow
  └── QWidget (central)
      └── QVBoxLayout（余白16px・間隔12px）
          ├── QSplitter (horizontal)
          │   ├── QFrame (左ペイン、objectName="card")
          │   │   └── QVBoxLayout（余白12px・間隔10px）
          │   │       ├── QHBoxLayout (ヘッダー行)
          │   │       │   ├── QLabel("マップ", subtle)
          │   │       │   ├── QSpacerItem
          │   │       │   └── QPushButton("GPXを開く", open_gpx_button)
          │   │       ├── QLabel (map_matching_status_label、既定非表示)
          │   │       ├── MapWidget          ← stretch: 1
          │   │       └── OffsetWidget       ← fixed height
          │   └── QFrame (右ペイン、objectName="card")
          │       └── QVBoxLayout（余白12px・間隔10px）
          │           ├── QHBoxLayout (ヘッダー行)
          │           │   ├── QLabel("動画", subtle)
          │           │   ├── QSpacerItem
          │           │   └── QPushButton("動画を開く", open_video_button)
          │           ├── QLabel (video_info_label、既定非表示)
          │           ├── QVideoWidget       ← stretch: 1
          │           ├── CustomTimeline     ← fixed height ~80px
          │           └── QHBoxLayout (transport_layout、間隔8px。19章)
          │               ├── QPushButton("◀ 逆再生", reverse_button)
          │               ├── QPushButton("⏮ コマ戻り", step_back_button)
          │               ├── QPushButton("⏸ 一時停止", pause_button)
          │               ├── QPushButton("⏭ コマ送り", step_forward_button)
          │               ├── QPushButton("▶ 再生", play_button)
          │               ├── QSpacerItem
          │               └── QLabel (現在時刻表示、current_time_label)
          └── QHBoxLayout (ボタン行、間隔8px)
              ├── QSpacerItem
              └── QPushButton("📤 Export && Test", objectName="primaryButton")

  初期ウィンドウサイズ: 1280 x 720
  左右ペインの初期比率: 1:1
  配色・QSSの詳細は16章（UIテーマ）、open_gpx_button/open_video_buttonの
  強調表示・map_matching_status_labelの詳細は17章、
  video_info_label・チェックボックスのチェックマークの詳細は18章、
  transport_layout（再生制御ボタン群）の詳細は19章を参照


9. map.qml の構成（2026-07-19、Step30でLeaflet版から全面置き換え）

----------------------------------------------------------------

  地図表示はQtLocationのOSMプラグイン（QML）によるネイティブ実装に
  置き換えた。Python側の呼び出しはQWebEnginePage.runJavaScript()に
  よるJS文字列組み立てから、QQuickWidget.rootObject()経由のQML
  function直接呼び出しに変わったが、MapWidgetクラスの公開メソッド名・
  シグネチャ（load_gpx_route/update_route_ranges/update_marker/
  hide_marker/clear）はLeaflet版から変更していない（main_window.py側の
  呼び出しコードは無変更）。

  import QtQuick
  import QtLocation
  import QtPositioning

  Map {
      id: root
      width: 800
      height: 600

      plugin: Plugin {
          id: osmPlugin
          name: "osm"

          // Qt標準のosmプラグインは、デフォルトのactiveMapTypeが
          // Qt社のプロキシ経由Thunderforestタイル（APIキー要求の
          // 透かし入り）になっている（Qt 6.11で実機確認）。
          // osm.mapping.host単体では効かず、osm.mapping.custom.hostで
          // 独自のCustomMapタイプを追加した上で、Map.activeMapTypeを
          // 明示的にそのタイプへ切り替える必要がある。
          PluginParameter { name: "osm.useragent"; value: "GPX-VSync/1.0" }
          PluginParameter {
              name: "osm.mapping.custom.host"
              value: "https://tile.openstreetmap.org/"
          }
          PluginParameter {
              name: "osm.mapping.custom.copyright"
              value: "&copy; OpenStreetMap contributors"
          }
      }

      center: QtPositioning.coordinate(35.0, 135.0)
      zoomLevel: 13

      // 操作性（パン・ズーム）。Map型はデフォルトでは対話操作に
      // 対応していないため、Qt公式Interactivityサンプルに準拠して
      // 明示的に追加（Step30-9、2章参照）
      property geoCoordinate startCentroid

      PinchHandler {
          id: pinchHandler
          target: null
          onActiveChanged: if (active) {
              root.startCentroid = root.toCoordinate(pinchHandler.centroid.position, false)
          }
          onScaleChanged: (delta) => {
              root.zoomLevel += Math.log2(delta)
              root.alignCoordinateToPoint(root.startCentroid, pinchHandler.centroid.position)
          }
          onRotationChanged: (delta) => {
              root.bearing -= delta
              root.alignCoordinateToPoint(root.startCentroid, pinchHandler.centroid.position)
          }
          grabPermissions: PointerHandler.TakeOverForbidden
      }

      WheelHandler {
          id: wheelHandler
          acceptedDevices: Qt.platform.pluginName === "cocoa" || Qt.platform.pluginName === "wayland"
              ? PointerDevice.Mouse | PointerDevice.TouchPad
              : PointerDevice.Mouse
          rotationScale: 1 / 120
          property: "zoomLevel"
      }

      DragHandler {
          id: dragHandler
          target: null
          onTranslationChanged: (delta) => root.pan(-delta.x, -delta.y)
      }

      property var routeLatLngs: []
      property var routeSegments: []

      function loadRoute(latlngs) {
          routeLatLngs = latlngs;
          var flags = [];
          for (var i = 0; i < latlngs.length; i++) flags.push(true);
          updateRouteRanges(flags);
          if (latlngs.length > 0) {
              var coords = [];
              for (var i = 0; i < latlngs.length; i++) {
                  coords.push(QtPositioning.coordinate(latlngs[i][0], latlngs[i][1]));
              }
              root.visibleRegion = QtPositioning.rectangle(coords);  // Leaflet版fitBounds相当
          }
      }

      // inRangeFlagsの切り替わり目でセグメントに分割する。継ぎ目が
      // 途切れないよう、直前の点も新セグメントの先頭に含める
      // （Leaflet版splitIntoSegments/renderRouteSegments相当）。
      function updateRouteRanges(inRangeFlags) {
          var segments = [];
          var current = null;
          for (var i = 0; i < routeLatLngs.length; i++) {
              var flag = inRangeFlags[i];
              if (!current || current.inRange !== flag) {
                  if (current) segments.push(current);
                  current = { inRange: flag, path: [] };
                  if (i > 0) {
                      current.path.push(QtPositioning.coordinate(
                          routeLatLngs[i - 1][0], routeLatLngs[i - 1][1]));
                  }
              }
              current.path.push(QtPositioning.coordinate(routeLatLngs[i][0], routeLatLngs[i][1]));
          }
          if (current) segments.push(current);
          routeSegments = segments;
      }

      property var markerCoordinate: QtPositioning.coordinate(0, 0)
      property bool markerVisible: false

      function updateMarker(lat, lon) {
          markerCoordinate = QtPositioning.coordinate(lat, lon);
          markerVisible = true;
          root.center = markerCoordinate;  // Leaflet版map.panTo相当（spec.txt 5-2節）
      }

      function hideMarker() {
          markerVisible = false;
      }

      function clearMap() {
          routeLatLngs = [];
          routeSegments = [];
          markerVisible = false;
      }

      MapItemView {
          model: root.routeSegments
          delegate: MapPolyline {
              line.width: 3
              line.color: modelData.inRange ? "#16a34a" : "#6b7280"
              path: modelData.path
          }
      }

      MapQuickItem {
          coordinate: root.markerCoordinate
          visible: root.markerVisible
          anchorPoint.x: markerShape.width / 2
          anchorPoint.y: markerShape.height / 2
          sourceItem: Rectangle {
              id: markerShape
              width: 16; height: 16; radius: 8
              color: "#f03"; opacity: 0.8
              border.color: "red"; border.width: 1
          }
      }

      onSupportedMapTypesChanged: {
          for (var i = 0; i < supportedMapTypes.length; i++) {
              if (supportedMapTypes[i].name === "Custom URL Map") {
                  activeMapType = supportedMapTypes[i];
                  return;
              }
          }
          if (supportedMapTypes.length > 0) {
              activeMapType = supportedMapTypes[supportedMapTypes.length - 1];
          }
      }

      // QtLocationのMap型はLeafletと異なりattributionを自動描画しない
      // ため、OSMタイル利用ポリシーに従い手動でオーバーレイ表示する。
      Rectangle {
          anchors.right: parent.right
          anchors.bottom: parent.bottom
          width: attributionText.implicitWidth + 8
          height: attributionText.implicitHeight + 4
          color: "#ccffffff"
          Text {
              id: attributionText
              anchors.centerIn: parent
              text: "© OpenStreetMap contributors"
              font.pixelSize: 11
              color: "#333333"
          }
      }
  }

  Python側からの呼び出し例（app/map_widget.py）:
    self._call_qml("loadRoute", latlngs)               # rootObject().loadRoute(latlngs)
    self._call_qml("updateRouteRanges", in_range)
    self._call_qml("updateMarker", lat, lon)
    self._call_qml("hideMarker")
    self._call_qml("clearMap")

  QQuickWidget特有の注意点（実機検証で判明。QWebEngineViewとの違い）:
    ローカルファイルのQMLはsetSource()が同期的にStatus.Readyへ遷移する
    （QWebEngineViewのloadFinishedのような非同期待ちが基本的に発生
    しない）。そのためMapWidgetの_pending_callsキューイング機構は
    通常のアプリ起動フローでは実質的に使われない（防御的な実装として
    維持）。map_readyシグナルも同様にコンストラクタ内で同期的に
    発火しうるため、ウィジェット生成後に外部から接続しても捕捉できない
    可能性がある点に注意（現状main_window.pyはこのシグナルを使用して
    いない）。


10. mapillary_toolsとの連携
----------------------------------------------------------------

  Step 8にて mapillary_tools 0.14.7 を実際にインストールし、
  `mapillary_tools video_process_and_upload --help` で正確な
  CLIオプションを検証済み（ドキュメント頼りではなく実コマンドで確認）。

  本ツールの出力ファイルを使ったアップロードコマンド（出力ダイアログに表示）:

    mapillary_tools video_process_and_upload <output.mp4> \
        --geotag_source exif \
        --video_start_time <YYYY_MM_DD_HH_MM_SS_sss> \
        --video_sample_distance 3 \
        --video_sample_interval -1 \
        --filetypes image

  --filetypes imageは24章で追加（2026-07-18）。付けないと
  mapillary_tools自身が「動画のサンプル画像と動画ファイル自体の両方を
  処理・アップロードしてしまう可能性がある」と警告する（実機検証では
  単一動画ファイルを渡す本ツールの使い方で実害は確認できなかったが、
  --filetypes imageを付けても結果が変わらず警告のみ消えることを
  確認済みのため、mapillary_tools自身の推奨に従い付与する）。

  --geotag_source を採用した経緯（GPX直接参照方式からの変更）:
    当初は --geotag_source gpx + --geotag_source_path <出力GPX> という
    構成だったが、検討の過程で「動画自体にGPSを埋め込む方が
    Mapillaryのメインストリームな使われ方（GoPro等のGPS内蔵動画を
    直接アップロードする運用）に近い」という指摘を受け、4-8節の
    CammEncoderで動画自体にCAMM形式のGPSトラックを埋め込む方式に
    変更した。実機検証で、自前生成したCAMMトラックを正しく読み取れ、
    座標が期待値と完全に一致することを確認済み（詳細は4-8節）。
    この方式では --geotag_source_path（外部GPXファイルの指定）は
    不要になる。ただし出力されるGPXファイル自体は、バックアップ・
    手動確認用として引き続き生成する（spec.txt 5-6節）。

  サンプリング方式・geotag_sourceについて（フェーズ2で
  時間ベース+camm → 距離ベース+exif に変更。15章のタイムラプス対応
  調査で判明した内容に基づく）:
    `mapillary_tools video_process` は内部で「sample_video」（動画から
    静止画を抽出しEXIFに時刻を書く）→「process」（--geotag_sourceで
    指定した方法でジオタグ付けする）の2段階に分かれている。
    2026-07-13の実機・ソースコード調査（mapillary_tools 0.14.7の
    sample_video.py・camm/camm_parser.py・geotag/geotag_images_from_gpx.py
    を直接確認）で以下が判明した:
      - `--video_sample_distance -1 --video_sample_interval N`
        （時間ベース）でのsample_videoは、CAMM/GPS情報を一切参照せず
        単純に "video_start_time + フレーム番号×interval" で時刻を
        決める（sample_video.py の _sample_single_video_by_interval）。
        後続の process --geotag_source camm が別途CAMMを読み直して
        位置を付与するが、その際 use_image_start_time=True の実装が
        CAMMサンプルのtime_gps_epochを保持したまま扱わず、素の
        geo.Point型に作り直してしまう（geotag_images_from_gpx.py）ため、
        タイムラプス動画（15章）では time_gps_epoch が失われ、
        圧縮されたネイティブ時間ベースの誤った時刻になってしまう。
      - 一方 `--video_sample_distance N --video_sample_interval -1`
        （距離ベース）でのsample_videoは、CAMMの各GPS点を直接読み、
        `interp.get_gps_epoch_time()`（sample_video.py内、コメントに
        "Try to use GPS epoch time if available (for timelapse videos)"
        とある）でtime_gps_epochが取得できればそれを直接採用して
        画像EXIFに位置・時刻を書き込む。この時点で位置・時刻付けは
        完結しているため、後続processでは --geotag_source exif
        （EXIFの値をそのまま信用する）を指定し、camm による再読込・
        上書きをさせない。
      - 旧Type 5（MIN_GPS）のCAMM実装では距離ベースサンプリングが
        "No GPS data found from the video" で失敗していたが、
        Type 6（GPS、time_gps_epoch付き）に切り替えたところ問題なく
        動作することを実機で確認した（4-8節）。
      - 実機検証（2026-07-13）: 実世界0.5秒間隔・fps=30相当
        （time_scale=15）のタイムラプスを模したCAMM Type6テスト
        ファイルで、上記の距離ベース+exif方式ではMAPCaptureTimeが
        正しく実世界の時間軸（例: 25秒間隔の点が25秒間隔のまま）に
        展開されることを確認。誤った時間ベース+camm方式では
        "distance gap too large" 警告と共に移動速度が実際の約15倍に
        見える壊れ方をすることも合わせて確認した。
      - 通常動画（video_time_scale=1.0）でも同じ距離ベース+exif方式で
        回帰が無いことを実機確認済み（15章）。そのため通常動画・
        タイムラプス動画を問わず、常にこの方式を使う（動画の種類に
        応じてコマンドを出し分ける必要はない）。

  --video_start_time について（必須。ファイル内メタデータに依存しない）:
    トリミング後の動画の実際の録画開始時刻
    （video_creation_time + playback_ms_to_real_ms(video_start_ms,
    video_time_scale)、UTC。15章のタイムラプス対応込み）を
    "YYYY_MM_DD_HH_MM_SS_sss" 形式（ミリ秒精度、例: 2026_07_12_01_00_02_500）
    で指定する。Exporter.get_video_start_time_str()（4-9節）で計算し、
    build_mapillary_tools_command() から使う。
    理由: 4-8節の通り、MP4Boxでのトラック追加処理により動画の
    creation_timeメタデータが不正な値（処理実行時刻）に上書きされて
    しまうため、動画ファイル内のメタデータには一切頼らず、
    コマンド引数で明示的に正しい時刻を渡す設計とする。実機検証で
    --video_start_time を指定すれば、ファイル内のcreation_timeが
    不正でも正しくジオタグできることを確認済み。

  注意: GPXのタイムスタンプ・CAMMトラックの時刻は動画の実際の
  録画時刻に合わせてあるため、--interpolation_offset_time は
  不要（本ツールで同期済みのため）。

  出力後のローカル検証について（5-10節・4-13節・4-14節）:
    アップロード自体には「公開せずデータ十分性だけ判定する」専用
    モードが無いことを2026-07-13にmapillary_tools 0.14.7のソース
    （uploader.py の finish_upload()）を直接確認して把握した。
    `--dry_run` はサーバーへ一切送らないローカルシミュレーション、
    `--nofinish` はサーバーへ送るが `finish_upload` API呼び出しを
    スキップするだけで、どちらもサーバー側のデータ検証結果は得られ
    ない。実質的な検証はアップロード前のローカル処理
    （`video_process`、ネットワーク送信なし）で完結しているため、
    本ツールはこれを出力操作に自動統合する（4-13節）。


11. エラーハンドリング
----------------------------------------------------------------

  - FFmpegが見つからない場合: 起動時チェックしてダイアログ表示
  - MP4Box（GPAC）が見つからない場合: 起動時チェックしてダイアログ表示
    （FFmpegと同様。brew install gpac を案内する）
  - 動画にcreation_timeがない場合: 手動入力フォームを表示
    （初期値はGPX開始時刻のローカル時刻換算値をプリフィル）
  - GPXとオフセット適用後に時刻範囲が重複しない場合: 警告を表示し、
    出力ボタンを無効化する（重複するまで出力不可）
  - ネットワーク不通時（OSMタイル取得失敗）: マップ無表示でも動作継続
  - マップマッチングAPI（13章）に到達できない場合: 1チャンク目失敗なら
    元座標のまま、2チャンク目以降の部分失敗ならそのチャンクのみ
    元座標のまま、GPX読み込み自体は継続する（5-9節・4-11節）
  - mapillary_toolsが見つからない場合（出力後ローカル検証時）:
    起動時チェックの対象にはせず（FFmpeg/MP4Boxと異なり必須外部
    ツールではないため）、出力完了時に検証をスキップし、その旨を
    完了メッセージに表示するのみとする（5-10節・4-13節）
  - タイムラプス設定が有効な場合（video_time_scale != 1.0）:
    フェーズ2（15章）でCAMM Type6のtime_gps_epoch対応により出力・
    Mapillary連携も通常動画と同様に動作するため、特別なブロックは
    行わない。GPXとオフセット適用後の実時刻範囲（video_time_scale
    考慮済み）が動画Start〜Endと重複しない場合にのみ、上記と同じ
    ロジックで出力ボタンが無効化される。


12. 開発・実行手順
----------------------------------------------------------------

  # 環境構築（.venvはリポジトリ直下 gpx-vsync/.venv に統一。
  # 2026-07-19、Step29のPySide6移行に伴いvideo_gpx_sync/.venvから
  # 移設した。理由: ユーザーの開発運用がリポジトリ直下でのvenv
  # activateに統一されているため）
  cd gpx-vsync                  # リポジトリ直下
  brew install ffmpeg gpac
  python -m venv .venv
  source .venv/bin/activate
  pip install -r video_gpx_sync/requirements.txt

  # 実行
  cd video_gpx_sync
  python main.py

  # requirements.txt (video_gpx_sync/requirements.txt)
  PySide6>=6.11.1
  gpxpy>=1.6.2
  requests>=2.32.0

  # デスクトップからの起動（簡易ランチャー）
  video_gpx_sync/GPX-VSync.app を Finder でダブルクリックすると、
  リポジトリ直下（video_gpx_syncの1階層上）の .venv を activate した
  上で、video_gpx_sync に cd してから main.py が起動する。Desktop等への
  エイリアス作成は任意（元の video_gpx_sync/GPX-VSync.app 自体を
  移動すると相対パス解決が壊れるため、移動する場合はエイリアス
  （シンボリックリンクではなくFinderの「エイリアスを作成」）を使うこと）。
  FinderからのGUI起動ではHomebrewのPATHが通っていないため、
  起動スクリプト内で /opt/homebrew/bin を明示的にPATHへ追加している。


13. マップマッチングAPI連携（外部サービス仕様）
----------------------------------------------------------------

  姉妹プロジェクト ../gpx-editor（React/JS版マップマッチング機能）と
  同じ外部API・パラメータを採用する。2026-07-13にAPI公式ドキュメント
  （valhalla.github.io/valhalla）およびFOSSGIS e.V.の利用ポリシーを
  直接確認済み（1章参照）。

  エンドポイント:
    POST https://valhalla1.openstreetmap.de/trace_attributes
    （FOSSGIS e.V. 運営の公開デモサーバー。本番サービスではなく
    デモ・検証用途と明記されている点に注意）

  リクエストボディ（JSON）:
    {
      "shape": [{"lat": <float>, "lon": <float>}, ...],
      "costing": "bicycle",
      "shape_match": "map_snap",
      "search_radius": 50,
      "filters": {
        "attributes": ["matched.point", "matched.type"],
        "action": "include"
      }
    }
    - shape: 1リクエストあたり50点以下（4-11節のCHUNK_SIZE）
    - costing: "bicycle"固定（本ツールの想定用途が自転車走行のため。
      gpx-editorと同一）
    - shape_match: "map_snap"固定（GPSトレースを道路網へスナップする
      モード。他に"edge_walk"/"walk_or_snap"があるが未使用）
    - search_radius: 50（メートル。道路候補の探索半径。API仕様上の
      上限は100m）

  リクエストヘッダ:
    Content-Type: application/json
    X-Client-Id: gpx-vsync
    （公開デモサーバーのfair-useポリシーで、公開アプリからの
    リクエストには識別用ヘッダーの付与が推奨されている。
    gpx-editor側には未実装だが、今回のPython実装では追加する）

  タイムアウト: 1リクエストあたり30秒（4-11節のmatch_chunk）

  レスポンスボディ（JSON、抜粋。filtersで絞り込み済み）:
    {
      "matched_points": [
        {"lat": <float>, "lon": <float>, "type": "matched"},
        {"lat": <float>, "lon": <float>, "type": "interpolated"},
        {"type": "unmatched"},
        ...
      ],
      ...
    }
    - matched_points は shape と同じ順序・同じ点数で返る
    - type:
        "matched"      道路上に正しくスナップできた点
        "interpolated" 前後のmatched点から補間された点
        "unmatched"    スナップできなかった点（lat/lonフィールドなし）
    - 本ツールでは matched/interpolated の座標のみ採用し、
      unmatchedは元座標を保持する（4-11節の apply_matched_points()）

  レート制限・fair-use（FOSSGIS e.V.の公開ポリシーより）:
    - 1ユーザーあたり1リクエスト/秒、サーバー全体で100リクエスト/秒
    - 公開アプリは Github Discussions での告知と X-Client-Id
      ヘッダーでの識別が求められている
    - 本サーバーはデモ・検証用途であり本番サービスとしての
      安定性は保証されないため、5-9節・11章のエラーハンドリング
      （タイムアウト時は元座標へフォールバック）を必ず実装すること
    - 2026-07-14変更: 当初はRDP間引きによりチャンク数が少なく
      （多くの場合1チャンクのみ）実質的に1req/秒を超えることは
      稀だったが、間引きを廃止した（4-11節）ことでチャンク数が
      増えるため、match_route()にMIN_REQUEST_INTERVAL_SEC=1.0の
      明示的なレート制限を追加し、チャンク送信間隔が1秒を
      下回らないようにした

  参考: gpx-editorのspec.txt 17-2章・10章にも同一APIの仕様が
  ドキュメント化されている（本プロジェクトの設計はそれを踏襲）。


15. タイムラプス動画対応
----------------------------------------------------------------

  背景（spec.txt 5-11節）:
    タイムラプス動画（例: 実世界0.5秒間隔で撮影したフレームを
    29.97fps等の通常フレームレートでコンテナ化したもの）は、
    「動画の再生位置(ms) = 実世界の経過時間」という、本ツールの
    時刻同期ロジック全体が前提としている等速関係が成り立たない
    （上記の例では、再生1秒 ≒ 実世界約15秒相当になる）。

  タイムラプスの自動判定について（2026-07-13、WebSearch/WebFetch
  による実地調査済み。記憶に頼った実装をしないというプロジェクト
  ルールに基づく）:
    - MP4/MOVコンテナには「タイムラプスである」ことを示す標準的な
      メタデータフィールドは存在しない。ffprobeで汎用的に読み取れる
      項目はない。
    - GoPro純正機の未加工(raw)ファイルにはGPMFメタデータストリーム
      内に `GoPro:Rate` タグ（例: "2_1SEC" = 0.5秒間隔）が存在するが、
      (a) ffprobe単体では読めずGPMF専用パーサが必要、
      (b) GoPro Player/Studioでの書き出しや再エンコードで消失する、
      という制約があり、本ツールが読み込む動画（編集・変換済みの
      可能性が高い）には期待できない。
    - 「音声トラックが無い」ことはタイムラプス動画によくある特徴だが、
      無音の通常動画も存在するため誤検知リスクが高く、自動補正の
      根拠にはできない（弱いヒントとしてのみ利用する）。
    - 結論: 確実な自動判定は一般に不可能。ユーザーによる明示指定を
      基本とし、音声トラック無しの場合のみ注意喚起ヒントを表示する
      （4-1節 load_video()）。
    参考: The Quirks of GoPro Metadata (GPMD) | Trek View
    (https://www.trekview.org/blog/gopro-camera-modes-metadata-tests/),
    Turning a GoPro TimeLapse Video into Photo Frames | Trek View
    (https://www.trekview.org/blog/turn-gopro-timelapse-video-into-timelapse-images/)

  フェーズ1のスコープ（プレビュー同期・強制同期・再生レート）:
    - AppState.video_time_scale（再生1msあたりの実世界ms数。通常
      動画は1.0）を導入し、以下に適用した:
        GPXHandler.interpolate_position()（プレビューのマーカー位置）
        MainWindow.on_force_sync_clicked()（強制同期ボタン）
        MainWindow._apply_playback_rate()（自動再生の速度）
    - この時点ではGPXHandler.export_trimmed() / get_points_for_camm() /
      _build_output_points() / _raw_time_range() / has_overlap()は
      変更せず、CAMM埋め込み・Mapillary連携（Export & Testボタン）は
      タイムラプス設定有効時は無効化した（フェーズ2で解消。下記）。

  フェーズ2: CAMM Type 6 (time_gps_epoch) によるMapillary連携対応
  （2026-07-13、実機検証で設計を確定・実装済み）:

    調査のきっかけ: 「動画にGPSを埋め込む設計にしたので、埋め込む
    GPS情報のタイミングさえ合っていれば動画自体のfpsとは無関係に
    正しく扱えるのでは」「Mapillaryはタイムラプス撮影
    （例: 自転車で0.5秒間隔）を元々想定しているはず」という指摘を
    受け、mapillary_tools 0.14.7のソースコードを直接確認した。

    発見1（sample_video.py）: `_sample_single_video_by_distance()`
    内に "Try to use GPS epoch time if available (for timelapse
    videos)" というコメント付きの分岐があり、GPS点が
    `get_gps_epoch_time()`（絶対UTCエポック秒）を持っていれば、
    動画のfps・PTSと無関係にそれをそのまま画像のMAPCaptureTimeとして
    採用する。この仕組みはCAMM Type 6（GPS。telemetry.CAMMGPSPointの
    time_gps_epochフィールド）でのみ機能し、当時使っていたType 5
    （MIN_GPS）には時刻フィールド自体が無いため常にフォールバック
    （video_start_time + 動画のネイティブ時間）に落ち、タイムラプス
    動画では圧縮された誤った時刻になっていた。

    発見2（サンプリング方式の依存関係）: `video_process`は
    「sample_video」→「process」の2段階に分かれており、
    time_gps_epochが実際に使われるのは **距離ベース**
    （--video_sample_distance N --video_sample_interval -1）の
    sample_videoのみ。**時間ベース**
    （--video_sample_distance -1 --video_sample_interval N、
    本ツールが元々使っていた方式）のsample_videoはCAMM/GPSを一切
    参照しない別のコードパスであり、後続のprocess --geotag_source
    camm が改めてCAMMを読み直して上書きするが、その実装
    （geotag_images_from_gpx.py の use_image_start_time=True 分岐）は
    time_gps_epochを保持しないただのgeo.Pointに作り直してしまうため、
    タイムラプス動画では時刻が壊れる。よって
    「距離ベースサンプリング + --geotag_source exif」への変更が
    必須と判明した（10章）。

    発見3（旧Type 5で距離ベースが失敗していた理由）: 4-8節の通り、
    旧実装ではCAMM Type 5で距離ベースサンプリングを試すと
    "No GPS data found from the video" で失敗し、時間ベースに
    回避していた（implement.txt旧版に記録あり）。Type 6へ切り替えた
    ところ、この問題が再現せず正常に動作することを実機で確認した
    （根本原因の完全な特定はできていないが、Type 6への切り替えで
    実用上解消したため、これ以上の深追いはしていない）。

    実機検証結果（2026-07-13、合成CAMM Type6テストファイルで確認）:
      - 実世界0.5秒間隔・fps=30相当（time_scale=15、ネイティブ10秒の
        動画が実世界150秒分のデータを表す想定）のタイムラプスで、
        distance方式+--geotag_source exifなら、MAPCaptureTimeが
        正しく実世界の時間軸（25秒間隔の点は25秒間隔のまま）に展開
        され、MapillaryOutsideGPXTrackErrorも発生しないことを確認。
      - 同じテストファイルで --geotag_source camm のままにすると、
        time_gps_epochが失われて時刻が圧縮され、
        MapillaryOutsideGPXTrackError多発・移動速度異常
        （"distance gap too large"警告）という壊れ方をすることも
        合わせて確認（詳細はtests/test_mapillary_validator.pyの
        test_validate_export_succeeds_for_timelapse_video）。
      - 通常動画（time_scale=1.0）でも同じdistance+exif方式で
        既存のTest 5相当の結果と一致し、回帰が無いことを確認した。

    設計変更点（4-6節・4-8節・4-9節・4-13節・10章に反映済み）:
      - camm_encoder.py: CAMM Type 5 → Type 6（time_gps_epoch付き）
      - gpx_handler.py get_points_for_camm(): 戻り値に epoch_time を
        追加。relative_msは「動画自身の圧縮されたネイティブな
        再生タイムライン上の位置」（real_ms_to_playback_ms()で
        実世界時間から逆変換）のまま維持し、epoch_timeで実時刻を
        別途伝える、という2軸に分離した設計とした
      - exporter.py・mapillary_validator.py: コマンドを
        `--geotag_source camm --video_sample_distance -1
        --video_sample_interval 2` から
        `--geotag_source exif --video_sample_distance 3
        --video_sample_interval -1` に変更
      - main_window.py: タイムラプス設定有効時の出力ボタン強制無効化
        を撤廃。GPXとの実時刻範囲重複判定（has_overlap、
        video_time_scale考慮済み）のみで判定する

  再生レート同期（実機検証済み、2026-07-13）:
    タイムラプス設定有効時、MainWindow._apply_playback_rate()で
    player.setPlaybackRate(1.0 / video_time_scale) を設定する
    （例: 0.5秒間隔・29.97fps → 約0.067倍速 = 見かけ上2fps相当）。
    実機検証（PyQt6/Qt6.11、macOS、FFmpegベースのQt Multimedia
    バックエンド）で、0.0667倍速のような極端に遅いplaybackRateでも
    エラー無く動作し（Error.NoError）、player.position()が経過時間に
    ほぼ比例して進行することを確認した（6秒間の実時間再生で
    約371ms分のPTSが進行、理論値約406msと概ね一致。タイマー精度上の
    誤差の範囲）。音声は極端な低速再生では意味を持たないため、
    タイムラプス有効時は自動的にミュートする。
    この方式では「▶ 再生」ボタンでの自動再生が実時間ベースになる
    ため、圧縮倍率が大きいタイムラプスほど視聴に長い実時間がかかる
    （例: 15倍圧縮なら動画本編4分の内容を実時間で約60分かけて
    再生することになる）。タイムライン上のドラッグによる手動シークは
    再生レートの影響を受けず瞬時に位置を移動できるため、確認作業
    自体はドラッグ操作で行うことを想定している。

  UIの視認性改善（2026-07-13。動画読み込み時の確認ダイアログ追加）:
    フェーズ1で追加した常設の「タイムラプス動画」設定
    （動画タイムラインの下、4-16節TimelapseWidget）が、実際に使って
    もらったところ「どこにあるかわからない」というフィードバックを
    受けた。headless環境でMainWindowを実際に起動しスクリーンショットを
    撮って確認したところ、ウィジェット自体は正しく描画されていたが、
    動画プレビューという視線が集まる領域から離れた、小さく目立たない
    場所に配置されていたことが原因と判明した。
    当初は「動画を開く」ネイティブファイルダイアログ自体に
    チェックボックスを追加することを検討したが、Qtの調査により
    macOSのネイティブファイルダイアログにはカスタムウィジェットを
    直接追加できず（DontUseNativeDialogでQt標準スタイルの
    ダイアログに切り替える必要があり、Finder風の見た目が失われる）
    ことが判明したため、代わりに動画選択直後に表示される軽量な
    確認ダイアログ（_prompt_timelapse_settings()、4-1節）を追加する
    方式を採用した。常設コントロール自体は、読み込み後に設定を
    変更したい場合のために引き続き残している。


16. UIテーマ（外観の刷新）
----------------------------------------------------------------

  背景（spec.txt 4章「外観」節）:
    2026-07-15、ユーザーより「PyQt6のデフォルトスタイルのままで
    手作り感がチープ、他のgpxシリーズ（gpx-navi、gpx-editor、
    gpx-vcycl）とのバランスを考えたデザインにしてほしい」との
    要望を受けた。実装前に他シリーズの現物を調査した:
      - gpx-editor（Webアプリ、Vite/React）: パープル基調
        （--accent: #9333ea系）、system-uiフォント、角丸カードUI、
        light/dark両対応のCSSカスタムプロパティ設計
      - gpx-vcycl（Webアプリ）: サイクルコンピュータ風の常時ダーク
        HUD、青/赤アクセント。design-preview.htmlは配色検討用の
        未使用ファイルで実運用CSSではない
      - gpx-navi: React Native製モバイルアプリで技術系統が異なる
    gpx-vsyncは「撮影後にファイルを処理するツール」であり、常時HUD
    表示のgpx-vcyclよりも同じくツール系のgpx-editorに近い立ち位置と
    判断し、カードUI・角丸・ライト/ダーク対応という構造は
    gpx-editorを踏襲しつつ、コンセプトカラーはユーザー指定により
    グリーン系（他シリーズのパープル・青赤とは別の、本ツール固有の
    アクセントカラー）とした。

  配色パレット（app/theme.py Palette。4-17節）:
                    Light                 Dark
      accent        #16a34a               #4ade80
      accent hover  #15803d               #22c55e
      accent bg     rgba(22,163,74,.10)   rgba(74,222,128,.15)
      accent border rgba(22,163,74,.5)    rgba(74,222,128,.5)
      bg            #ffffff               #16171d
      card bg       #f7faf8               #1c211d
      border        #e5e4e7               #2e303a
      text          #08060d               #f3f4f6
      text subtle   #6b6375               #9ca3af

  ライト/ダーク自動切替（実機検証済み、2026-07-15）:
    Qt6.11のQApplication.styleHints().colorScheme()でmacOSの
    システム外観を検出できることをoffscreen環境で確認した
    （Qt.ColorScheme.Light/Dark/Unknown列挙体が存在することを確認。
    offscreenプラットフォーム自体はUnknownを返すため、テスト実行時は
    Lightにフォールバックする実装とした。実機のcocoaプラットフォーム
    では実際のシステム設定に応じてLight/Darkが返る）。

  適用方法:
    main.py で QApplication 生成直後に apply_theme(app) を1回呼び、
    app.setStyleSheet() でアプリ全体にQSSを適用する（個々の
    ウィジェットへの個別setStyleSheet()は行わない）。

  MainWindowのレイアウト変更（4-1節・8章）:
    - 左右ペインをQWidgetからQFrame（objectName="card"）に変更し、
      QSSでカード状（角丸8px・薄ボーダー・背景差）に描画されるように
      した
    - Exportボタンにのみ objectName="primaryButton" を付与し、
      アクセントグリーンで塗りつぶした強調ボタンとして区別する
      （Play/Pause等の副次的な操作はボーダーのみの控えめな見た目の
      ままとする）
    - 全体・各ペイン内・ボタン行の余白/間隔を調整（詰まりすぎていた
      レイアウトに16px/12px/8px程度の余白を追加）

  CustomTimeline（app/video_widget.py）・地図ルート線
  （2026-07-19以降はassets/map.qml、当時はassets/map_template.html）
  の配色統一:
    QSSの適用範囲外であるQPainterによる直接描画（タイムラインの
    選択範囲ハイライト）と、地図側のポリライン（GPXルート線）に
    ついても、既存の任意色（青系）からアクセントグリーン
    （#16a34a、ハイライトは半透明の rgba(22,163,74,150)）に変更し、
    視覚的な一貫性を持たせた。現在位置を示す赤い縦線、Start/End
    ハンドルの色分け（開始=緑・終了=赤）、現在地マーカー（赤）は
    機能的な意味を持つ配色のため維持している。

  副次的に発見した既存の不具合（本改修中に修正）:
    Exportボタンのラベル文字列 "\U0001f4e4 Export & Test" 内の
    単体の "&" が、QPushButtonにおいてQtのニーモニック（アクセラ
    レータキー）記号として解釈され、表示が崩れる（"& T"が消えて
    次の文字に下線が付く）不具合をheadlessスクリーンショットで
    発見した。テーマ変更とは無関係の既存不具合だが、見た目の
    改善作業の一環として "&&"（リテラルな"&"1文字として表示される
    Qtのエスケープ表記）に修正した。QPushButton.text()は
    エスケープ前の生文字列（"&&"含む）を返す仕様のため、対応する
    テスト（test_export_button_label_is_export_and_test）の
    アサーションもあわせて更新した。


17. カードヘッダーの開くボタン・マップマッチング結果表示
----------------------------------------------------------------

  背景（spec.txt 4章「外観」節、5-9節末尾）:
    2026-07-15、ユーザーより以下2点の要望を受けた:
      a) 「GPXを開く」「動画を開く」が現状メニューバーにしか無く、
         ウィンドウ内にボタンを置きたい
      b) マップマッチングはタイムアウトすることもあるため、
         全点取得完了しているかどうかを編集画面のどこかに表示したい
    a)については、独立した上部ツールバー案と、左右カードのヘッダーに
    それぞれ配置する案を提示し、後者（カードヘッダー案）が選ばれた。
    未読み込み状態を目立たせる方法として、既存のprimaryButton
    （16章）のQSSをそのまま流用し、objectName切り替え＋
    style().unpolish()/polish()で動的に強調のON/OFFを切り替える
    方式を採用した（headlessスクリーンショットで塗り色が実際に
    切り替わることを実機確認済み）。

  b)の判定ロジックについて（app/main_window.py
  _update_map_matching_status()）:
    match_route()（4-11節）のstatusフィールドは "完了" | "エラー" |
    "キャンセル" の3値だが、"エラー" は「チャンク処理自体は全て成功
    したが、スナップ対象の点が1つも無かった（n_snapped == 0）」場合
    にも返る（既存ロジック: final_status = "完了" if n_snapped > 0
    else "エラー"）。そのため、成否の判定はstatus文字列ではなく
    result.error（Noneかどうか）を優先させた:
      - error is None: 全チャンク成功（state="ok"、緑）。
        n_snappedが0でも「元々スナップ不要だった」だけであり失敗
        ではないため、成功表示とする
      - error is not None かつ status=="キャンセル": 1チャンク目の
        失敗により自動キャンセルされたケース（state="error"、赤）
      - error is not None かつ status!="キャンセル": 2チャンク目以降
        の一部が失敗したが処理は継続したケース（state="warn"、
        オレンジ）。エラー詳細はQLabel.setToolTip()でホバー時にのみ
        表示し、文言のパース（"chunk N: ..."形式）には依存しない

  表示先: 左カード（マップ）のヘッダー行の直下に
  map_matching_status_label（QLabel）を配置。GPX未読み込み時は
  非表示（setVisible(False)）、マップマッチング完了後に表示する。

  補足（2026-07-15追加、同要望のフォローアップ）:
    map_matching_status_labelが緑色「✓ マップマッチング完了」で
    表示されているにも関わらずn_snappedが点数より少ない
    （例: 382点中370点）ケースについて、「最後のチャンクが集計
    から漏れているのでは」との懸念を受けた。合成データ（382点、
    最終チャンクを意図的に32点＝端数にした上で、そのうち12点を
    "unmatched"として返すダミーAPI）で実機検証したところ、
    match_route()のfor c in range(total_chunks)ループは最終
    チャンク（端数）も含めて欠落なく処理され、n_snapped=370・
    matched_points総数=382（欠落なし）となることを確認した
    （tests/test_map_matcher.pyには回帰目的の専用テストは追加して
    いない。都度の疑義に対する調査記録として本節に残す）。
    すなわちerror is None（緑色表示）の場合の「点数 < 全体数」は
    チャンク処理の失敗や欠落ではなく、Valhalla側が一部の点を
    道路に自信を持ってスナップできなかった（type="unmatched"）
    ことを意味し、元のGPS座標がそのまま採用される、正常な
    フォールバック動作である。


18. 動画情報表示・チェックボックスの見た目改善
----------------------------------------------------------------

  背景（spec.txt 4章「外観」節）:
    2026-07-15、UIを実際に使ったユーザーから2点の要望を受けた:
      a) タイムラプス設定チェックボックスのチェック時の見た目が
         「ただの緑塗りつぶし」でチェックマークが無く、いまいち
      b) 動画側にもfps・タイムラプス設定などの素性を示すテキストを
         読み込み後に表示してほしい。表示位置は左（マップ側）の
         行に上下位置を揃えてほしい

  a) チェックボックスのチェックマーク（app/theme.py, assets/icons/check.svg）:
    QSSのQCheckBox::indicator:checkedにimage: url(...)でSVG
    アイコンを指定できるかを実装前にheadless環境で実機検証し
    （白いチェックマークが緑地の上に正しく描画されることを
    スクリーンショットで確認済み）、以下の方式を採用した:
      - assets/icons/check.svg: 白いチェックマーク、透過背景
        （viewBox 0 0 16 16、strokeベースのシンプルな意匠）
      - theme.py: CHECKMARK_ICON_PATH（__file__基準の絶対パス）を
        定義し、build_stylesheet()のQCheckBox::indicator:checked
        ルールに image: url({CHECKMARK_ICON_PATH}); を追加
    QSSのurl()は相対パスだと解決基準が不明瞭になりやすいため、
    実行時に__file__から解決した絶対パスを埋め込む方式とした
    （macOS単一プラットフォーム対応のため、パス区切り文字の
    差異は考慮不要）。

  b) 動画情報表示（app/main_window.py）:
    - video_info_label（QLabel）を右カード（動画）のヘッダー行の
      直下に配置。左カードのmap_matching_status_labelと同じ位置
      （ヘッダー行の直後）に置くことで、ヘッダー行の高さが左右で
      揃っている前提のもと、上下位置が自然に一致する
    - _update_video_info_label(interval_sec: float):
        state.video_path is None: 非表示
        self._video_fps is None: "fps不明"、state="warn"
        それ以外: f"{fps:.2f}fps"、state="ok"
                  state.video_time_scale != 1.0 の場合は
                  f"（{interval_sec:g}sタイムラプス）" を追記
      文字色はmap_matching_status_labelと同じstateプロパティ方式
      （setProperty("state", ...) + style().unpolish()/polish()、
      17章のQLabel[state="ok"/"warn"/"error"]スタイル）を流用する
      （2026-07-18追加。当初はstateを設定しておらず既定の黒文字
      だった）
    - on_timelapse_changed()（15章）の末尾から呼ぶことで、
      load_video()内のtimelapse_widget.reset()や
      _prompt_timelapse_settings()由来のsetChecked/setValue、
      および読み込み後の常設タイムラプス設定チェックボックスの
      手動変更、いずれの経路でも自動的にラベルが更新される
      （on_timelapse_changed自体は複数の経路から発火する既存の
      シグナルハンドラであり、新たな呼び出し箇所を追加する必要が
      なかった）


19. 再生制御ボタン群の動画カード内移動・逆再生/コマ送り機能
----------------------------------------------------------------

  背景（spec.txt 5-5節）:
    2026-07-15、ユーザーより「ウィンドウ左下の再生・一時停止ボタンを
    動画枠内のシークバーの下に移動し、逆再生・コマ戻り・一時停止・
    コマ送り・再生の5ボタンにしてほしい。コマ送り・コマ戻りは1フレーム
    単位で」との要望を受けた。

  逆再生の実現方式（実機検証済み、2026-07-15。2026-07-18に不具合修正で
  ペーシング方式へ変更）:
    QMediaPlayer.setPlaybackRate()に負値（例: -1.0）を設定しても
    エラーにはならないが、実際にはplaybackRate()の値が黙って0.0に
    丸められることをQt6.11のFFmpegベースマルチメディアバックエンド
    （macOS、offscreen環境）で確認した。すなわちこのバックエンドは
    ネイティブな逆再生に対応していない。Qt6公式ドキュメント
    （QMediaPlayer.playbackRate）でも"Negative playback rates are not
    supported."と明記されている（2026-07-18確認）。
    代替として、player.position()を少しずつ減算していく疑似逆再生方式を
    採用しているが、その実装は2段階の変遷を経ている。

    [初版 2026-07-15、不具合あり]
    QTimer（REVERSE_TICK_MS=40ms間隔）でsetPosition()を無条件に連投
    する方式。setPositionを短い間隔で連続呼び出ししてもクラッシュせず
    position()の値が単調に減少することは実機検証済みだったが、
    「position()の値（＝マップのマーカー位置）は更新されるのに、
    映像フレームは1枚も描画されないまま真っ黒・またはフリーズして
    見える」という不具合が2026-07-18の実機検証で発覚した。原因は、
    Qt6のマルチメディアバックエンド（FFmpeg/AVFoundation、Qt公式
    フォーラムで同様の報告多数）が、一時停止中に短い間隔で連投された
    setPosition()のどれも描画完了まで処理しきれず、次のシークに
    上書きされ続けてしまうことだった。シークバーをドラッグしての
    スクラブ操作でも、ドラッグ中は映像が固まり指を離した瞬間に描画
    される、という同一原因の不具合が確認された。
    なお、この際に暫定対処として毎ティックplayer.play()→pause()を
    挟む方式を試したが、状態遷移が余計に増えることでかえって描画が
    完全に止まる（常に真っ黒）症状に悪化したため撤回した。

    [現行版 2026-07-18]
    根本原因（連投されたシークが描画完了を待たずに上書きされ続ける）
    に対処するため、VideoWidget内に「直前のシークの映像フレーム描画が
    実際に完了するまで、次のシークを発行しない」ペーシングの仕組みを
    導入した（app/video_widget.py）。この仕組みは逆再生専用ではなく、
    video_widget.seek()（シークバーのドラッグ・コマ送り/戻りが使用）と
    VideoWidget._on_start_changed()/_on_end_changed()
    （Start/Endハンドルドラッグ時の現在地クランプ）からも共通で
    使われる、根本原因を共有する3箇所すべてに対する修正である。

    - VideoWidget.seek_settled（新設シグナル、引数なし）:
      直前のシーク要求について、実際に映像フレームの描画が完了した
      （またはSEEK_FRAME_TIMEOUT_MS=400ms待っても完了しなかった
      ためタイムアウトで打ち切った）ことを通知する
    - VideoWidget._request_seek(ms): 公開の seek() や
      _on_start_changed()/_on_end_changed() から呼ばれる内部エントリ
      ポイント。既に処理中のシークがあれば、新しい目標位置で
      self._pending_seek_ms を上書きするだけに留め、setPosition()は
      呼ばない（＝連投せず、間の目標は間引かれる。最後に要求した
      位置には必ず収束する）
    - VideoWidget._start_next_paced_seek(): self._pending_seek_ms を
      対象にplayer.setPosition()を実行し、self._seek_in_flight=True、
      SEEK_FRAME_TIMEOUT_MSのタイムアウトタイマーを開始する
    - VideoWidget._on_seek_frame_ready(): player.videoSink().
      videoFrameChanged（Qt6公式API。setVideoOutput()呼び出し後は
      videoSink()が有効な値を返すことを実機・ヘッドレス環境で確認済み）
      またはタイムアウトタイマーのtimeoutに接続。処理中でなければ
      無視（通常再生中の毎フレームのvideoFrameChangedと衝突しない
      ためのガード）。保留中の目標があれば
      _start_next_paced_seek()で次のシークへ進み、なければ
      seek_settled を発行する
    - main_window.py側の逆再生ロジック:
        on_reverse_clicked(): video_widget.pause()の上で
          self._reverse_active = True にし、
          self._reverse_last_step_time = time.monotonic() -
          REVERSE_INITIAL_STEP_MS/1000.0 で基準時刻を初期化してから
          _reverse_step() を呼ぶ
        _reverse_step():
          now = time.monotonic()
          elapsed_ms = (now - self._reverse_last_step_time) * 1000.0
          self._reverse_last_step_time = now
          step_ms = max(round(elapsed_ms * _current_playback_rate()), 1)
          new_pos = video_widget.player.position() - step_ms
          new_pos <= timeline.start_ms() の場合:
            _stop_reverse()（_reverse_active = False）
            video_widget.seek(timeline.start_ms())
          それ以外: video_widget.seek(new_pos)
        _on_seek_settled(): video_widget.seek_settled 受信時に
          _reverse_active が真なら _reverse_step() を呼び、次の
          ステップへ進める（＝1ステップの実際の描画完了を待ってから
          次のステップへ、というペーシングを逆再生でも実現）
        _stop_reverse(): self._reverse_active = False。
          on_play_clicked() / on_pause_clicked() / _step_frame() /
          load_video() / closeEvent() の冒頭で呼び、逆再生と他の操作が
          同時に走らないようにする（QTimerを使わなくなったため
          isActive()での判定はできず、_reverse_active フラグで判定する）

      1ステップで戻す距離（step_ms）について（2026-07-18、2回目の
      不具合修正）:
        ペーシング方式に変更した直後は、旧REVERSE_TICK_MS（当時
        REVERSE_STEP_MSに改名済み）=40ms固定のままだった。これは
        「40ms間隔のQTimerで連投する」旧方式（上記参照）の名残の値を
        そのまま流用していたため、「逆再生が2〜3秒に1コマしか
        進まない」不具合が発生した。
        原因: H.264等の圧縮動画は数十〜数百フレームに1回しかキー
        フレーム（単独復号可能なIフレーム）を持たず、任意位置への
        シークは「目標より前の直近キーフレームまで戻ってから、目標
        位置までのフレームを順にデコードし直す」処理になる
        （Qt Forumでも同様の記述あり）。40msという極めて小さい戻り幅
        では、毎ステップの目標がほぼ同じキーフレーム区間に収まり
        続けるため、ほぼ同じ区間を毎回デコードし直すだけで実質的な
        進行がごくわずか、という非効率な状態になっていた。
        スクラブ操作（5-3節）がこの問題を起きないのは、ドラッグ中は
        目標位置がマウスの動きに応じて大きく飛ぶため、1回のシーク
        コストに対して進む距離が大きく、キーフレーム区間をまたいで
        効率よく進めるため。
        対策として、step_msを固定値ではなく「直前のステップに実際に
        かかった実時間（time.monotonicで計測）× _current_playback_rate()」
        から動的に算出する方式に変更した。処理が遅ければその分次回は
        大きく戻るようになるため、スクラブ操作と同様に、遅い環境・
        動画でも実時間に見合った速度に自然と収束する（1ステップ目のみ
        比較対象がなく、REVERSE_INITIAL_STEP_MS=40ms相当を初期値として
        使う）。_current_playback_rate()を使う点は変更なく、タイムラプス
        設定有効時は順再生と同じ体感速度で巻き戻る（4-1節参照）。

  コマ送り・コマ戻り（app/main_window.py _step_frame(direction)）:
    - 1フレーム分のミリ秒数は round(1000 / self._video_fps) で算出する
    - video_time_scale（タイムラプス設定）の影響は受けない。
      「1フレーム」は常に動画ファイル自身のネイティブなフレーム単位を
      指すため（15章のタイムラプス時間変換とは無関係の概念）
    - video_widget.seek()（既存、Start/Endで自動クランプ）を使って
      移動するため、Start/Endの範囲外には出ない
    - self._video_fps is None（fps取得失敗）の場合は _step_frame()は
      何もしない。_update_frame_step_buttons_enabled()を
      __init__時・load_video()でのfps取得直後に呼び、
      step_back_button/step_forward_buttonの有効・無効を同期する

  レイアウト変更（8章参照）:
    play_button・pause_button・current_time_labelをウィンドウ下部の
    ボタン行から、動画カード内・CustomTimelineの直下（新設の
    transport_layout）へ移動した。ボタン順序は「◀ 逆再生」「⏮ コマ戻り」
    「⏸ 一時停止」「⏭ コマ送り」「▶ 再生」（ユーザー指定順）。
    ウィンドウ下部のボタン行にはExportボタンのみ残る。


20. マップ上でのクロップ領域の可視化
----------------------------------------------------------------

  背景（spec.txt 5-2節）:
    2026-07-15、ユーザーより「シークバー上のStart/Endで定義される
    出力領域と、クロップされる領域の区別を左の地図上でもつけたい。
    クロップ領域（＆動画のタイムスタンプから外れている領域）は、
    枠線緑のグレーの太線にしたい」との要望を受けた。

  「クロップ領域」の定義:
    GPXHandler._raw_time_range()（4-6節、has_overlap()と共通のロジック）
    が、動画のStart/End・オフセット・タイムラプス倍率から算出する
    「出力範囲に対応するGPX記録時刻の範囲」を基準とする。この範囲外の
    GPXポイントが「クロップされる領域（＝動画のタイムスタンプから
    外れている領域）」であり、Start/Endのトリミングとタイムスタンプの
    ずれの両方を単一の概念として扱っている（ユーザーの要望文にある
    「クロップ領域」と「タイムスタンプから外れている領域」は、
    実装上は同じ_raw_time_range()判定の裏表であるため、区別せず
    1つのin_rangeフラグとして表現する）。

  実装:
    - GPXHandler.classify_points_in_range()（4-6節）:
      get_all_points()と同じ順序でin_range: list[bool]を返す
    - MapWidget.update_route_ranges(in_range)（4-2節）:
      JS側のupdateRouteRanges()を呼ぶ
    - map.qml（9章。2026-07-19以前はmap_template.html）:
      loadRoute()はrouteLatLngsに緯度経度を保持するよう変更
      （Start/End操作のたびに全点を再送しないため）。
      updateRouteRanges(inRangeFlags)は、隣接する点のinRangeFlagが
      切り替わる箇所でセグメントに分割し、区間ごとに再描画する
      （セグメントの継ぎ目が視覚的に途切れないよう、直前の点を
      新セグメントの先頭にも含める。Leaflet版splitIntoSegments()と
      同一ロジックをQMLへ移植）
    - 配色: 範囲内は従来通り緑の太線（#16a34a、line.width 3）。範囲外は
      グレーの太線（#6b7280、line.width 3）
      （Leaflet版は実機でrunJavaScript()を実行し検証。QML版もStep30-2で
      実際にMapPolyline描画のスクリーンショットを確認し、セグメントの
      色分け・継ぎ目が途切れないことを検証済み）
    - MainWindow._update_route_range_highlight()（4-1節）: GPX・動画の
      両方が読み込み済みの場合のみclassify_points_in_range()→
      update_route_ranges()を呼ぶ。on_start_changed() / on_end_changed() /
      on_offset_changed() / on_duration_changed() / on_timelapse_changed()
      から呼ばれ、Start/End・オフセット・タイムラプス設定の変更の
      たびに塗り分けを再計算する

    配色の修正（2026-07-15、初版リリース直後）:
      当初は範囲外を「太い緑の下地(weight5)+細いグレー(weight3)」の
      重ね描きで「緑の枠線が覗くグレーの太線」として実装したが、
      実際に表示すると縁取りの分だけ範囲外の線が範囲内より太く見えて
      しまい、意図したイメージと異なるとのフィードバックを受けた。
      縁取りを廃止し、範囲内と同じweight 3のグレー単色に変更した。


21. 出力前のGPSカバレッジ確認・自動クロップ
----------------------------------------------------------------

  背景（spec.txt 5-6節）:
    2026-07-15、ユーザーより「オフセット調整後、結果的にGPSデータの
    ない動画フレームが存在する可能性があると思うが、その場合の処理は
    どうなっているか」との質問を受けた。実装（GPXHandler.has_overlap()
    ・_build_output_points()）とmapillary_tools側のソース
    （.venv内のgeo.py、Interpolator._interpolate_at_segment_idx()）を
    確認したところ、以下が判明した:
      - has_overlap()は「Start-End全体がGPXの記録範囲でカバーされて
        いるか」ではなく「少しでも重なっているか」という緩い判定
        しか行っていない
      - GPXHandler側の出力処理（_build_output_points()）は、GPXの
        記録範囲外の境界について補間（_interpolate_at()）が失敗する
        ため、単にその区間のデータを出力しない（エラー・警告なし）
      - mapillary_tools側のInterpolatorは、CAMM等の点列の最初/最後の
        点より前後の時刻について、明示的に直線外挿
        （linear extrapolation）で位置を推定する
        （_interpolate_at_segment_idx()の
        "# Extrapolating before the first point" コメント参照）。
        つまり処理自体は破綻しないが、実測ではなく推定値になる
    この調査結果を受け、ユーザーから「警告を出し、同意が得られれば
    GPSの無い動画部分をクロップする」という対応を依頼された。

  実装:
    - GPXHandler.clip_to_gps_coverage()（4-6節）: Start/EndをGPXの
      実記録範囲にクロップした値を返す（クロップ不要ならそのまま返す）
    - MainWindow._confirm_and_apply_gps_coverage_crop()（4-1節）:
      GPX・動画未読み込み時、またはクロップ不要（既に完全カバー）の
      場合は確認ダイアログを出さずTrueを返す。クロップが必要な場合は
      QMessageBox.question()で
      「GPSデータのない動画フレームが存在するため、動画をクロップ
      します。よろしいですか？」（OK/キャンセル）を表示する:
        OK: video_widget.timeline.set_start()/set_end()でシークバーの
            表示を更新した上で、既存のon_start_changed()/
            on_end_changed()（4-1節）を直接呼んでMainWindow側の
            state・出力ボタン状態・20章のルート塗り分け表示も
            連動して更新する。最後にvideo_widget.seek()で現在の
            再生位置を新しいStart-End範囲にクランプし、Trueを返す
        キャンセル: 何も変更せずFalseを返す
    - on_export_clicked()（4-1節）: 冒頭で
      _confirm_and_apply_gps_coverage_crop()を呼び、Falseなら
      出力先ディレクトリ選択より前の時点で処理を中断する


22. 出力ダイアログの保存ダイアログ化
----------------------------------------------------------------

  背景（spec.txt 5-6節）:
    2026-07-15、ユーザーより「Export&Testをクリックした時開くダイアログの
    実行ボタンが『Open』になっているんだけど、本来なら『保存』にしたいし、
    ファイル名の入力もできるほうがいい。デフォルト名は今と変えずに」との
    要望を受けた。従来はQFileDialog.getExistingDirectory()（フォルダ
    選択専用ダイアログ）で出力先フォルダのみを選択し、動画・GPXの
    ファイル名は両方ともExporter側で自動生成していたため、実行ボタンの
    ラベルがQtの標準仕様で「Open」になっていた。

  実装:
    - QFileDialog.getExistingDirectory() → QFileDialog.getSaveFileName()
      に変更（app/main_window.py on_export_clicked()）。実行ボタンは
      OS標準の「保存」ラベルになる
    - 初期表示ファイル名は Exporter.default_video_filename(state)
      （app/exporter.py、新設）で、変更前と同じ命名規則
      f"{元動画ファイル名}_synced.mp4" を返す
    - フィルタは "MP4 Files (*.mp4)"
    - Exporter.export()の第2引数を output_dir から video_output_path
      （動画の保存先フルパス）に変更した。GPXファイルは
      video_output_pathの親ディレクトリに、従来通り
      generate_output_path()で自動生成した名前
      （f"{元GPXファイル名}_synced.gpx"、重複時は連番付与）で出力する

    仕様変更点（ユーザーへの事前説明・承認済み）:
      動画ファイルは保存ダイアログでユーザーが明示的にパス・ファイル名を
      指定する形になったため、従来あった「重複時は自動的に_2を付与する」
      連番ロジックは動画には適用されなくなった。保存先に同名ファイルが
      既にある場合は、OS標準の「上書きしますか？」確認ダイアログが
      表示される（Save系ダイアログの一般的な挙動に合わせた）。GPX側の
      自動連番ロジック（generate_output_path()）自体は変更していない。


23. GPXデータの取得元統合・読み込み順序ルール撤廃・検証モード廃止
----------------------------------------------------------------

  背景（spec.txt 5-1節・5-12節）:
    2026-07-15、ユーザーより「検証モードを導入した経緯があるが、
    よくよく考えるとGPXと動画を一緒に読み込むだけで通常モードと
    変わらないのでは」との指摘を受けた。実装（VerificationWindow.
    open_video()）を確認したところ、実際にはGPXファイルを一切
    読み込まず、動画自体に埋め込まれたCAMM GPSトラック
    （camm_encoder.extract_gps_track()）を抽出して表示していることが
    判明した。つまり検証モードが確認していたのは「動画に実際に
    埋め込まれたGPSが正しいか」＝CAMM埋め込み処理自体の正しさで
    あり、外部GPXファイルとの同期確認とは異なる目的だった。

    ユーザーからの追加説明で、真の狙いは「CAMM埋め込み動画を読み込む
    ことで、GPXデータと動画データの両方を内部的に読み込んだことに
    してしまえば、以降の内部処理（マップ表示・オフセット調整・
    出力）を通常モードと完全に共通化できるはず」という統合案だと
    判明した。優先ルールについては、当初「明示的なGPXファイルが
    常に優先」という非対称案を提示したが、ユーザーからの指摘により
    「GPX・動画（のCAMM埋め込み）いずれの経路でも、最後に読み込んだ
    GPS付きファイルが常に優先される」という対称なルールに変更した。

  実装の骨子:
    a) camm_encoder.extract_gps_track()（4-8節）がtime_gps_epoch
       （実際の撮影時刻）を読み捨てずに返すよう変更（embed_gps_track()
       と対称な5要素タプルに）。これにより動画埋め込みGPSからも
       実時刻付きのGPXTrackPointを構築できるようになった
    b) GPXHandler.from_camm_points()（4-6節、新設）で、CAMM抽出点から
       実時刻付きのGPXHandlerを構築する。以降は通常のGPXHandler
       パイプラインをそのまま使い回せる（interpolate_position/
       has_overlap/classify_points_in_range/export_trimmed等）
    c) MainWindow._apply_gps_source()（4-1節、新設）で、GPXファイル
       読み込み・動画のCAMM埋め込み抽出のいずれの経路でも、
       state.gpx_handler/gpx_path/gpx_dataを無条件で上書きする
       共通処理に統一。呼ばれるたびに上書きすることで「最後勝ち」
       ルールを実現している
    d) MainWindow.load_video()の末尾近くで_try_load_embedded_gps()
       （4-1節、新設）を呼び、CAMM埋め込みがあれば自動的に
       _apply_gps_source()を呼ぶ。埋め込みが無い動画の場合は
       早期returnし、既存のGPXデータには一切触れない
    e) load_gpx()・load_video()双方の「相手が先に読み込まれている
       必要がある」というガード（QMessageBox警告＋return）を撤廃
       （4-1節）。open_video_action/open_video_buttonも常時enabled
       にし、_update_open_button_emphasis()の判定もGPX・動画で
       独立させた（17章の強調表示ロジックを23章向けに調整）
    f) dragEnterEvent/dropEvent（4-1節）も、拡張子が".gpx"以外は
       常にload_video()を試みるよう簡略化。「GPX未読み込み時は動画の
       ドロップを拒否する」という分岐を撤廃した
    g) _prompt_creation_time()（4-1節）のgpx_handler前提のassertを
       撤廃し、未読み込み時は現在時刻をデフォルトにフォールバックする
       よう変更（動画がGPXより先に読み込まれるケースに対応）
    h) VerificationWindow（app/verification_window.py）・
       interpolate_camm_points()（camm_encoder.py）・
       「ツール」メニュー・tests/test_verification_window.pyを削除
       （4-8節・4-10節）

  マップマッチングとの関係（5-9節）:
    動画のCAMM埋め込みから採用したGPSデータには、マップマッチング
    （5-9節・4-11節）を適用しない。既に処理済みの実走行データ
    （多くの場合、本ツール自身が過去にエクスポートした動画に
    埋め込んだもの、または既にマップマッチング済みの状態で
    埋め込まれたもの）であり、外部GPSデバイスの生データとは性質が
    異なるため。ステータス表示（左カード、5-9節と同じ場所）も
    データの出所に応じて文言を切り替える
    （_update_gps_source_status_for_camm()、4-1節。「✓ 動画に
    埋め込まれたGPSデータを使用（◯点）」）。マップマッチング結果
    表示（_update_map_matching_status()）と同じ
    _set_map_matching_status_label()共通ヘルパー（4-1節、新設）で
    テキスト・状態色（state="ok"/"warn"/"error"）・ツールチップを
    まとめて設定する。

  バージョン: 読み込み順序ルールの撤廃・検証モードの廃止という
  破壊的仕様変更のため、メジャー更新（0.20.1 → 1.0.0）とした。

  副次的に発見・修正した不具合（2026-07-15、リリース直後）:
    ユーザーが実際にエクスポート済みの動画（本ツール自身が過去に
    出力したCAMM埋め込み動画）を単体で読み込んだところ、マップ上の
    ルート線が全区間グレー（範囲外）表示になり、動画を再生しても
    マーカーが一切動かない不具合が報告された。

    原因調査（実機検証。ffprobeで問題の動画ファイルを直接確認）:
    MP4Boxでトラック（CAMM）を追加すると、動画コンテナのcreation_time
    メタデータ（format.tags.creation_time）が「MP4Boxを実行した
    処理実行時刻」で上書きされてしまうことを実機で再確認した
    （4-8節「制約3」に既存の既知事項として記載していたが、これまでは
    mapillary_toolsへの--video_start_time明示指定でのみ対策しており、
    本ツール自身がVideoHandler.get_creation_time()経由でこの値を
    信用してstate.video_creation_timeに設定する経路
    （_try_load_embedded_gps()、上記d)）については対策していなかった。
    23章の新機能で初めてこの経路が使われるようになったため、これまで
    表面化していなかった）。
    実機で合成テストデータ（sample_420p.mp4にCAMM埋め込み）を作成し
    再現したところ、埋め込み直後のCAMM済み動画のcreation_timeが
    テスト実行時刻（例: "2026-07-15T13:11:51.000000Z"）に化けており、
    元の動画自身の実際の撮影時刻（"2026-07-12T01:00:00.000000Z"）とは
    無関係な値になっていることを確認した。この誤ったcreation_timeを
    state.video_creation_timeとして使うと、raw_time_range()の計算
    全体が数日分ずれてしまい、GPX（CAMM由来）の全記録点が
    Start-End範囲外（classify_points_in_range()が全てFalse）・
    interpolate_position()の記録範囲外（Noneを返す）判定になっていた。

    修正: _try_load_embedded_gps()内で、CAMM抽出点のうち
    relative_msが最小（通常は0、動画の先頭フレームに対応）の点の
    epoch_timeを、state.video_creation_timeとして直接採用するよう
    変更した（ffprobe由来の値を上書きする）。CAMMデータ自身が
    「動画再生位置0msに対応する実時刻」を正確に保持しているため、
    コンテナメタデータより信頼できる情報源として扱う。この方針は
    本ツール自身がエクスポートした動画に限らず、任意のCAMM埋め込み
    動画に対して一般に妥当な設計（コンテナレベルのcreation_timeは
    秒単位精度に留まりがちで、各種ツールによる上書きの影響も
    受けやすいため、CAMMのepoch_timeの方が本質的に精度・信頼性が
    高い）と判断した。
    tests/test_main_window.pyに回帰テスト
    （test_camm_video_creation_time_uses_embedded_epoch_not_corrupted_metadata）
    を追加し、CAMM埋め込み動画読み込み後にstate.video_creation_time
    が正しいepoch由来の値になること、classify_points_in_range()が
    全区間Trueになること、interpolate_position()がNoneを返さない
    ことを確認した。

24. Mapillaryへのアプリ内アップロード機能
----------------------------------------------------------------

  背景（spec.txt 5-7節）:
    2026-07-18、ユーザーより「Export完了ダイアログに表示している
    mapillary_tools video_process_and_uploadコマンドを、アプリから
    直接実行できるUploadボタンをつけられないか」との要望を受けた。

  事前調査（実機、2026-07-18）:
    - この時点でユーザーの環境ではmapillary_toolsがPATHに通って
      おらず（pipxでインストール済みだが~/.local/binが$PATHに
      含まれていなかった）、`pipx ensurepath`での対処を別途案内した。
    - mapillary_tools 0.14.7を実際にインストールして--helpを確認し、
      video_process_and_uploadのpositional引数（video_import_path、
      import_pathは省略可）・--geotag_source等の既存オプションが
      現行バージョンでも有効であることを確認した。
    - mapillary_toolsは本ツールのrequirements.txtに記載された正式な
      依存パッケージではなく、ユーザーが別途PATHにインストールする
      外部CLIツールという位置づけである（is_mapillary_tools_available()
      がshutil.whichでのみ検出することからも明らか）。そのため
      Pythonライブラリとしてimportする設計は避け、既存の
      validate_export()（5-10節）と同じくsubprocess経由でCLIとして
      呼び出す設計とした。
    - 認証状態を事前にチェックする公式なCLIサブコマンドは存在しない
      （authenticate --helpにlist/status系オプションなし）。設定
      ファイルを直接パースすることもできるが、mapillary_tools内部
      実装（config.py）への依存になるため避け、未認証時はアップロード
      実行時のエラー出力（mapillary_tools自身のstderr）をそのまま
      表示する方針とした。
    - `mapillary_tools upload --help`で--dry_run（実際にはMapillary
      サーバーへ送信せずローカルの一時ディレクトリへシミュレーション
      する開発用オプション）の存在を確認し、自動テストではこれを
      使うことで実ネットワーク送信・実アカウント認証なしに
      upload_export()の正常系・異常系を検証できるようにした
      （tests/test_mapillary_validator.py）。

  実装の骨子:
    a) app/mapillary_validator.py:
       UploadResult（ok/errors/warnings）dataclassと、
       upload_export(video_path, video_start_time, user_name=None,
       should_cancel=None, dry_run=False)を追加。validate_export()
       と同じくsubprocess.Popen + ポーリングによるキャンセル対応・
       stderrからのERROR/WARNING行抽出パターンを踏襲する。
       stdin=subprocess.DEVNULLを明示し、万一mapillary_tools側が
       対話的プロンプトを出そうとしても即座にEOFで失敗し、GUIの
       バックグラウンドスレッドがハングしないようにしている。
       dry_run=Trueの場合のみ--dry_runを付与する（本番のアップロード
       では使わない、テスト専用の引数）。
    b) app/mapillary_upload_worker.py（新設）:
       MapillaryUploadWorker(QThread)。MapillaryValidationWorker
       と同じ構造（finished_upload(object)シグナル、
       request_cancel()、run()内でupload_export_implを呼ぶ）。
    c) app/mapillary_upload_dialog.py（新設）:
       MapillaryUploadDialog(QDialog)。MapillaryValidationDialog
       と同じ構造（不定形プログレスバー＋キャンセルボタン、
       finished_uploadでaccept()）。「キャンセルしても中断した
       時点までは再開可能」の案内文言を追加している
       （mapillary_tools側のレジューム機能を前提とした案内）。
    d) app/exporter.py Exporter.build_mapillary_tools_command():
       user_name: str | None = None を追加し、指定時は
       " \\\n    --user_name {user_name}" をテンプレート末尾に
       追記する（手動コピペ用コマンドとアプリ内アップロードの両方で
       同じ--user_nameが使われるようにするため）。
    e) app/main_window.py:
       - _build_menu()にファイルメニュー「Mapillaryユーザー名...」
         （mapillary_username_action）を追加。
         on_edit_mapillary_username_clicked()でQInputDialog.getText
         （現在値を初期表示、空欄でOKすると未設定に戻せる）を開き、
         _set_mapillary_user_name()で保存する。
       - _get_mapillary_user_name()/_set_mapillary_user_name()は
         QSettings経由でOS標準の設定ストレージ（macOSでは
         ~/Library/Preferences/）に永続化する。_mapillary_settings()
         で QSettings(QSettings.Format.IniFormat,
         QSettings.Scope.UserScope, SETTINGS_ORG, SETTINGS_APP) を
         明示的に使う。素の QSettings(org, app) だとmacOSでは常に
         NativeFormat（plist）に解決され、QSettings.setPath()による
         リダイレクトが効かずテストが実際のユーザー設定を汚染して
         しまうため（実機・テストで確認済み）、IniFormat+UserScopeを
         明示することでQSettings.setPath()が効くようにしている
         （tests/test_main_window.pyのisolated_qsettingsフィクスチャ、
         autouseで全テストに適用）。
       - on_export_clicked(): 出力完了後、
         is_mapillary_tools_available_impl()がTrueの場合のみ
         _run_local_validation()（5-10節、旧
         _validate_export_and_format_message()を検証実行部分と
         メッセージ整形部分に分割したもの）を呼ぶ。can_upload =
         （検証結果が存在し、かつresult.ok）で判定し、
         _show_export_complete_dialog()に渡す。
       - _show_export_complete_dialog(): 標準のQMessageBox.
         information()ではなくQMessageBoxインスタンスを直接構築し、
         addButton("Mapillaryへアップロード",
         QMessageBox.ButtonRole.ActionRole)でカスタムボタンを追加
         する。can_upload=Falseの場合はこのボタンをsetEnabled(False)
         にする。box.exec()後、box.clickedButton()がアップロード
         ボタンだった場合のみ_start_mapillary_upload()を呼ぶ。
       - _start_mapillary_upload(): 外部サービスへの公開的な
         アップロードという不可逆かつ他者に影響する操作のため、
         実行前にQMessageBox.question()で確認する（既定ボタンは
         Cancel）。OKされた場合のみMapillaryUploadWorker/
         MapillaryUploadDialogを生成・実行し（5-10節の
         _run_local_validation()と同じworker.start()→dialog.exec()→
         worker.wait()パターン）、結果を_show_upload_result()で
         成功（警告件数込み）・失敗（エラー内容）・キャンセルの
         3パターンに分けて表示する。

  テスト:
    - tests/test_mapillary_validator.py: upload_export()の成功
      （CAMM埋め込み動画、--dry_run）・GPS無しでの失敗・キャンセル・
      --user_nameが実際に渡っていることの確認（存在しないプロファイル
      名を指定した際のエラーメッセージに含まれることで間接的に検証）
      を追加。既存のvalidate_export()のテスト群と同じく
      mapillary_tools実機が必要なためpytestmark skipif配下。
    - tests/test_mapillary_upload_worker.py（新設）:
      test_mapillary_validation_worker.pyと同じ構造で、フェイクの
      upload_export_implを使い、mapillary_tools実機なしで
      Worker/Dialogの配線を検証する。
    - tests/test_exporter.py: build_mapillary_tools_command()への
      user_name引数追加のテスト（未指定時は--user_nameを含まない
      ことも確認）。
    - tests/test_main_window.py: ユーザー名の既定値・QSettings経由の
      永続化・別インスタンスをまたいだ永続化・メニュー操作（OK/
      Cancelそれぞれ）、出力完了ダイアログのアップロードボタンの
      有効/無効切り替え・クリック時の遷移、_start_mapillary_upload()
      の確認ダイアログCancel時に何も起きないこと・OK時にworkerが
      正しいuser_nameで呼ばれ結果メッセージが表示されることを検証。

  バージョン: 新機能追加のためAPP_VERSIONをマイナー更新
  （1.0.2 → 1.1.0）。

  追記（2026-07-18、同日中の追加修正）: 実機での--dry_run実行時、
  mapillary_tools自身が「Force the option "filetypes" to be "image"
  to avoid processing and uploading both the video samples and the
  videos themselves」という警告を出すことに気づいた。--filetypes
  ありなしの両方で実機検証した結果、単一動画ファイルを渡す本ツールの
  使い方では実害（動画ファイル自体の誤アップロード）は確認できな
  かったが、--filetypes imageを付けても結果（画像件数・アップロード
  件数）が完全に同一で警告のみ消えることを確認できたため、mapillary_
  tools自身の推奨に従い、以下3箇所すべてに--filetypes imageを追加
  した（10章の該当コマンド例も同時に更新）:
    - exporter.py MAPILLARY_TOOLS_COMMAND_TEMPLATE（手動コピペ用）
    - mapillary_validator.py upload_export()（アプリ内アップロード）
    - mapillary_validator.py validate_export()（5-10節ローカル検証。
      video_process単体でも同じ警告が出ることを実機確認したため
      あわせて追加）

25. GPX単体出力機能の廃止
----------------------------------------------------------------

  背景（spec.txt 5-6節）:
    2026-07-19、ユーザーより「Exportの際、動画とは別に元GPXの
    あるフォルダに`{元ファイル名}_synced.gpx`を出力していないか」
    という確認を受けた。調査の結果、確かに出力していることを確認。
    この機能は動画にGPS情報を内蔵する（CAMM形式）機能ができる前の
    「バックアップ・手動確認用」の名残であり、Mapillaryへの受け渡し
    には動画に内蔵されたGPSトラック（CAMM）のみで完結し、この
    GPXファイルは不要（implement.txt旧1733-1735行目にも同様の
    注記があった）。ユーザーの判断で撤去することになった。

  変更内容:
    - app/gpx_handler.py: export_trimmed()メソッドを削除
      （呼び出し元がExporter.export()のみだったため。共通ヘルパー
      _build_output_points()はget_points_for_camm()が引き続き使う
      ため維持）
    - app/exporter.py:
      - export()の戻り値をtuple[str, str]（動画・GPXパス）から
        str（動画パスのみ）に変更。GPX出力パス生成・
        export_trimmed()呼び出しを削除
      - GPX出力専用だったgenerate_output_path()を削除（動画の
        保存先は保存ダイアログの指定値をそのまま使うため、この
        連番付与ロジックはGPX出力にしか使われていなかった）
      - build_mapillary_tools_command()の未使用引数gpx_output_pathを
        削除（元々コマンドテンプレート内で参照されておらず、実質
        使われていなかった）
    - app/main_window.py: on_export_clicked()・
      _show_export_complete_dialog()からgpx_path関連の処理・
      完了ダイアログの「GPX: {path}」表示を削除
    - テスト: tests/test_gpx_handler.pyのexport_trimmedテスト、
      tests/test_exporter.pyのgenerate_output_path関連テスト・GPX
      書き出しアサーションを削除。関連テストの呼び出しシグネチャを
      更新（195件→192件）
    - spec.txt §1・§5-6: GPXファイル単体出力の記述を削除
    - implement.txt §4-6（GPXHandler）・§4-9（Exporter）:
      export_trimmed()関連の記述を削除

  バージョン: 出力ファイルが1つ減る仕様変更のためAPP_VERSIONを
  マイナー更新（2.1.2 → 2.2.0）。


================================================================
                              以上
================================================================
